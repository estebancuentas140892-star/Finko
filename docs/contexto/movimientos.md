# Ficha de contexto: Movimientos

> Revisado: 2026-08-11.

> Ver reglas de uso y plantilla en [`README.md`](README.md).

---

## Ledger unificado y accionable (dominio `movimientos`, TX.8a/TX.8b/MOV.1/MOV.2)

- **Objetivo**          : historial único de todo lo que movió dinero, **derivado** de las colecciones que ya existen (no hay log paralelo): `S.gastos`, `S.ingresosPuntuales`, `S.ahorro.aportes` y `S.transferencias`, normalizados a un shape común `Movimiento`. Dos superficies: el panel compacto "Actividad reciente" de Inicio y la vista completa `#movimientos`, agrupada por mes y paginada. Desde MOV.1, cada fila de la vista completa ofrece las acciones (editar/eliminar) que su dominio dueño ya sabe hacer. Desde MOV.2, la vista completa se puede filtrar por texto, dominio y rango de fechas.
- **Estado actual**     : estable. **MOV.2 cerrada** (2026-07-22, hallazgo de la auditoría de UX/producto: encontrar "ese pago de hace 4 meses" era scroll ciego, agravado porque PERF.1 pagina por lotes). **MOV.1 cerrada** el mismo día (hallazgos P4 y P3). **Amplía deliberadamente el alcance de TX.8b**, que entregó el ledger como solo-lectura: decisión de Esteban del 2026-07-22, registrada aquí y en el CHANGELOG, no revertida en silencio (regla 2.7). Antes: **TX.8a** (dominio nuevo + Actividad reciente en Inicio), **TX.8b** (vista completa en ruta propia), **MC.17c** (transferencias en el ledger, dirección neutra), **MC.17d** (rastro del 4x1000), **PERF.1** (paginación por lotes).
- **Verificado contra** : commit de MOV.2 (2026-07-22).

**Dónde vive**

| Pieza | Archivo | Ancla | Línea |
|---|---|---|---|
| Normalización por fuente | `modules/dominio/movimientos/logic.js` | `movimientosDesdeGastos()`, `movimientosDesdeIngresosPuntuales()`, `movimientosDesdeAportes()`, `movimientosDesdeTransferencias()` | ~60, ~81, ~104, ~131 |
| Combinado y ordenado | `modules/dominio/movimientos/logic.js` | `movimientosRecientes()`, `movimientosCompletos()` | ~159, ~179 |
| Panel compacto de Inicio (markup propio) | `modules/dominio/movimientos/view.js` | `renderActividadReciente()` | ~68 |
| Vista completa agrupada por mes | `modules/dominio/movimientos/view.js` | `renderMovimientosCompletos()`, `_agruparPorMes()` | ~319, ~156 |
| Fila del ledger + acciones (MOV.1) | `modules/dominio/movimientos/view.js` | `_renderMovimientoItem()`, `_ACCIONES_POR_TIPO` | ~170 |
| Paginación por lotes (PERF.1) | `modules/dominio/movimientos/view.js` | `_aplanarEntradas()`, `_agregarSiguienteLote()`, `cargarMasMovimientos()` | ~215, ~277, ~304 |
| Re-render por cambio de fuente | `modules/dominio/movimientos/index.js` | `_SECCIONES_FUENTE`, `initMovimientos()` | ~16, ~23 |
| Descripción resuelta (compartida por render y búsqueda, MOV.2) | `modules/dominio/movimientos/logic.js` | `descripcionMovimiento(m, cuentas)` | |
| Filtro puro por texto/dominio/fechas (MOV.2) | `modules/dominio/movimientos/logic.js` | `filtrarMovimientos(movimientos, {texto, dominio, desde, hasta, cuentas})` | |
| Barra de filtros de la vista completa (MOV.2) | `modules/dominio/movimientos/view.js` | `renderFiltrosMovimientos()`, `_DOMINIO_LABEL`, setters `setFiltroTexto/Dominio/FechaDesde/FechaHasta`, `limpiarFiltrosMovimientos()` | |
| Botón "Limpiar filtros" actualizable sin recrear los inputs (MOV.2) | `modules/dominio/movimientos/view.js` | `actualizarBotonLimpiarFiltros()`, slot `#movimientos-limpiar-slot` | |
| Wiring de los inputs de texto/fecha (no son `data-action` de clic, MOV.2) | `modules/dominio/movimientos/index.js` | `_wireFiltrosMovimientos()`, `_filtrarDominio()`, `_limpiarFiltrosMovimientos()` | |
| Contenedor de la barra de filtros | `index.html` | `#movimientos-filtros` (antes de `#lista-movimientos`) | ~535 |

**Recursos**: estilos `.list-item*` compartidos (`atoms.css`), `.movimientos-*`/`.actividad-reciente__*` (`domain.css`), y `.filtros-bar`/`.chip`/`.chip--active` reusados de Gastos (MOV.2, ningún componente nuevo); símbolos `i-edit`, `i-trash`, `i-transferencia`; tejas por dominio vía `tejaCategoria(icono, dominio)`.

**Dependencias y relaciones**: `movimientos` **no importa ningún dominio** (ADN 10): los íconos salen de `core/constants.js` y las acciones se delegan por nombre de `data-action`, no por import. `index.js` escucha `state:change` de `_SECCIONES_FUENTE` (`gastos`, `ingresosPuntuales`, `ahorro`, `transferencias`); como `infra/crud.js` emite `state:change` con el nombre de la colección, cualquier alta/edición/borrado en un dominio dueño repinta el ledger solo.

**Riesgos**:

- **`m.dominio` NO es el dueño de los datos, es una etiqueta visual.** Un gasto de categoría "Deudas" o "Gastos fijos" lleva `dominio: 'compromisos'` (para colorear su teja) pero su registro vive en `S.gastos` y lo administra el dominio `gastos`. **Enrutar acciones por `m.dominio` mandaría la acción al dominio equivocado**: el enrutador correcto es **`m.tipo`**, que sí mapea 1:1 con la colección de origen (`gasto`→`S.gastos`, `ingreso`→`S.ingresosPuntuales`, `aporte`→`S.ahorro.aportes`, `transferencia`→`S.transferencias`). La tarjeta MOV.1 del BOARD proponía `m.dominio`; se corrigió en el análisis.
- **El ledger solo expone lo que el dueño ya sabe hacer; no inventa capacidades.** Hoy: `gasto` edita y borra, `ingreso` puntual solo borra, `aporte` solo borra, `transferencia` ninguna. El hueco de `transferencia` es de **MC.17f** (deshacer transferencia). El de `aporte`: Metas, Apartados e Inversión ya saben editar su registro completo (**EDIT.1**, cerrada 2026-08-04 en las 4 secciones), pero eso edita la meta/apartado/inversión, no la fila individual de aporte que el ledger lista; nadie ha construido edición de un aporte puntual. Cuando cualquiera de los dos exista, basta añadir su entrada en `_ACCIONES_POR_TIPO`: la fila no necesita más cambios.
- **Delegar (y no reimplementar) es lo que preserva la corrección del dinero.** `_eliminarGasto` devuelve el monto al saldo de la cuenta y revierte el abono de la deuda si era un gasto-abono; `_eliminarIngresoPuntual` revierte el crédito a la cuenta. Reimplementar el borrado en el ledger habría perdido esas reversas. Cualquier acción futura debe delegar igual.
- **Las acciones viven solo en la vista completa**, no en "Actividad reciente" de Inicio: son dos renderizadores distintos (`_renderMovimientoItem` vs. el markup propio de `renderActividadReciente`), y el panel de Inicio es un resumen compacto a propósito.
- **La paginación de PERF.1 pinta por lotes con `insertAdjacentHTML`**: cualquier cosa que agregue la fila debe ser HTML autocontenido (sin wiring por `id` ni listeners propios). Las acciones cumplen porque usan `data-action` delegado en `ui/actions.js`, que escucha en `document`.
- **El filtro se aplica sobre la fuente derivada, ANTES de agrupar/paginar (MOV.2), nunca sobre el DOM**: `renderMovimientosCompletos()` llama `filtrarMovimientos()` entre `_todosLosMovimientos()` y `_agruparPorMes()`. Filtrar después del windowing de PERF.1 sería incorrecto (con años de historial, ni siquiera están todos los nodos pintados).
- **Los handlers de texto/fecha NO repintan la barra de filtros completa, a propósito**: si `#movimientos-buscar` se recreara en cada tecla, el usuario perdería el foco y el cursor a mitad de palabra. Por eso esos dos handlers (`index.js`) solo llaman a `renderMovimientosCompletos()` (la lista). **Trampa real que esto generó y ya está corregida**: el botón "Limpiar filtros" vive en el MISMO `innerHTML` que se salta ese repintado, así que se quedaba sin aparecer cuando el único filtro activo era texto o fecha, hasta que algo más forzara un repintado completo (ej. cambiar de dominio). Se resolvió con un slot dedicado (`#movimientos-limpiar-slot` + `actualizarBotonLimpiarFiltros()`), que los 3 handlers (texto, desde, hasta) SÍ llaman. Cualquier futuro campo de filtro que no repinte la barra completa debe recordar llamarlo también.
- **El filtro de dominio SÍ usa `m.dominio` (no `m.tipo`), y es la decisión correcta acá**: para acciones (MOV.1) enrutar por dominio sería un bug (manda al handler equivocado); para un FILTRO es al revés, es lo que el usuario necesita: aislar "solo lo de Deudas" de "solo gasto cotidiano" es algo que `tipo` (que junta ambos bajo `'gasto'`) no puede distinguir.

**Cambios pendientes**: **MC.17f** (deshacer transferencia), en `docs/BOARD.md`. Editar un aporte puntual desde el ledger no tiene tarjeta propia todavía (ver la nota de arriba).

**Cambios realizados**:

- 2026-07-22 (**MOV.2**, hallazgo de la auditoría de UX/producto: encontrar "ese pago de hace 4 meses" era scroll ciego, agravado porque PERF.1 pagina por lotes): la vista completa gana búsqueda por texto, chips por dominio y rango de fechas. `logic.js`: `descripcionMovimiento(m, cuentas)` nuevo (extraído de una duplicación en `view.js`, resuelve "Origen → Destino" de una transferencia con los nombres vigentes) y `filtrarMovimientos(movimientos, {texto, dominio, desde, hasta, cuentas})` puro. `view.js`: `renderFiltrosMovimientos()` pinta el buscador + `.filtros-bar`/`.chip` (reusa el lenguaje de Gastos, cero componente nuevo) + rango de fechas en `#movimientos-filtros` (contenedor nuevo en `index.html`, antes de `#lista-movimientos`); `renderMovimientosCompletos()` filtra la fuente derivada antes de agrupar/paginar. `index.js`: `_wireFiltrosMovimientos()` cablea los inputs de texto/fecha a mano (no son `data-action` de clic), `_filtrarDominio()` maneja el chip. **Bug real detectado al verificar en la app y corregido en la misma rebanada**: el botón "Limpiar filtros" no aparecía con un filtro de solo texto o fecha, porque esos handlers no repintan la barra completa (perderían el foco); se resolvió con el slot `actualizarBotonLimpiarFiltros()` (ver Riesgos). 41 tests unitarios + 6 E2E nuevos. Verificado en la app real con 6 movimientos de 4 fuentes: buscar "mercado" aísla 1 fila sin perder el foco; el chip "Deudas" aísla el gasto de categoría interna; el rango de fechas funciona inclusive; "Limpiar filtros" restaura las 6 filas y vacía todos los campos. Ver CHANGELOG.
- 2026-07-22 (**MOV.1**, hallazgos P4 y P3 de la auditoría de UX/producto): la fila del ledger deja de ser solo-lectura. `_ACCIONES_POR_TIPO` (mapa de routing/UI, cero lógica) y `_renderAccionesMovimiento()` nuevos en `view.js`; la fila suma `.list-item__action` con los mismos `data-action` que cada dominio dueño ya registra (`editar-gasto`/`eliminar-gasto`, `eliminar-ingreso-puntual`, `ahorro-eliminar-aporte`). **Cero cambios en `logic.js`, `index.js` y CSS**: el `data-action` delegado en `ui/actions.js` escucha en `document`, así que funciona con el HTML inyectado por lotes de PERF.1, y el repintado ya lo daba `_SECCIONES_FUENTE` + los `state:change` que emite `infra/crud.js`. **La tarjeta del BOARD proponía enrutar por `m.dominio` y se corrigió a `m.tipo` en el análisis** (ver Riesgos: un gasto de categoría "Gastos fijos" lleva `dominio: 'compromisos'` pero vive en `S.gastos`). 8 tests unitarios + 3 E2E nuevos. Verificado en la app real: borrar un gasto de $80.000 desde el ledger devolvió el saldo de $500.000 a $580.000 (reversa aplicada por el handler de Gastos, no por el ledger) y la fila desapareció sola; editar abrió el form de Gastos con el monto cargado; la fila de "Pago: Arriendo" (teja de compromisos) enrutó correctamente a las acciones de gasto; la transferencia no ofreció ninguna. Ver CHANGELOG.
