# Ficha de contexto: Inicio (dashboard)

> Revisado: 2026-08-11.

> Ver reglas de uso y plantilla en [`README.md`](README.md).

---

## Estructura actual del Dashboard (bento grid)

- **Objetivo**          : pantalla principal e inamovible de la app; hoy muestra saldo (hero a ancho completo), accesos rápidos personalizables, paneles de vencidos/próximos y un resumen semanal.
- **Estado actual**     : estable, en evolución. **IN.7**, **IN.6a**, **CAL.1**, **TX.8a**, **TX.8b** e **IN.4a** cerradas; **IN.5** (2026-07-06) eliminó "Gasto rápido" y el panel "Gastos por organizar", dejando el hero a ancho completo. **PERF.1** (2026-07-06) paginó por lotes la vista completa de Movimientos (`#movimientos`). **PERF.2** (2026-07-06) memoizó `resumenSemanal()` y `movimientosRecientes()`/`movimientosCompletos()`; sin cambios de comportamiento visible en ninguno de los dos. **IV.2c** (2026-07-10, ADR 031): "Pendientes del mes" y "Próximas prioridades" ganan una etiqueta de texto con el tipo (`.dom-badge`, "el color nunca viaja solo") junto al icono+color de sección ya existentes; corrige un bug real donde un apartado en "Próximas prioridades" heredaba el color de "fijo" en vez del suyo propio. Rediseño mayor decidido en [ADR 028](../DECISIONS/028-inicio-centro-de-control.md) (**aprobado el 2026-07-05**): un rol por bloque, orden vertical definido. **Nota (2026-07-12):** la revisión formal de ese ADR ya existe y está aceptada: [ADR 034](../DECISIONS/034-inicio-v2.md) "Inicio v2" (diseño hifi de Esteban en `Iteración de specimen/design_handoff_inicio_v2/`), que reordena la pantalla (alertas primero, accesos+actividad fusionados al final), rediseña hero/pendientes/resumen y resuelve IN.6b con teja de iniciales (sin foto). **Actualización (2026-07-12):** las 7 rebanadas IN.8a a IN.8g ya cerraron; esta ficha describe el estado vigente en código con el diseño completo de "Inicio v2" implementado.
- **Verificado contra** : IN.9e (2026-08-02).

**"Pendientes del mes" ya no lista lo pagado, y ofrece pagarlo junto** (CAL.5b, 2026-08-02). Su fuente pasa de `detectarVencidosCompletos` (calendario puro: día de pago contra hoy, sin mirar gastos) a `vencidosSinPagar` de ese mismo dominio, que le cruza `estadoPagoMes`; una deuda con abono parcial sigue listada, solo sale en 'completo'. Con dos o más pendientes aparece "Pagar los N" (`data-action="inicio-pagar-lote"`), que emite `lote:abrir` y abre el modal de pago en lote **sobre el dashboard, sin navegar**: el lote es de Agenda y acá no se calcula ni se escribe dinero. Por eso el listener de `state:change` de Compromisos repinta el dashboard también con `section === 'gastos'`: pagar un fijo no toca la colección `compromisos`.

**Escritorio: iniciativa IN.9, cerrada** ([ADR 057](../DECISIONS/057-inicio-en-escritorio.md)). El ADR 034 diseñó Inicio contra un mockup de 390px y dejó escrito que el reparto de escritorio quedaba sin decidir; el ADR 057 lo decide: cero celdas de ancho completo, `span 4` y `span 6` (regla R78). Cerraron **IN.9a** (los dos avisos de "Atención hoy" comparten fila), **IN.9b** (8 filas de actividad en escritorio), **IN.9c** (el detalle por cuenta a columna propia, D3 del ADR, acota el 034 D4 a móvil), **IN.9d** (Accesos rápidos en fila propia + Resumen semanal y Actividad reciente en la fila final 6+6, D4 del ADR, acota el 034 D7 a móvil) e **IN.9e** (estado vacío propio de escritorio: pieza centrada de 620px con tres pasos, contra la guía de móvil que se diluía estirada a 990px; copy provisional, PI5). **Fuera de la iniciativa:** ID4 e ID7 del informe (ocultar `.perfil-inicio` y el carril de urgencias) dependen de una barra superior de escritorio que no existe en el código, registrada como INT.1.

**Riesgos vivos de la iniciativa:** IN.9c partió la máscara de privacidad en dos celdas (acordeón móvil + columna de escritorio); IN.9d hizo lo mismo con Accesos/Actividad (fusión móvil + columna/celda de escritorio). Las dos garantías las cubren tests dedicados (IN.8c/IN.9c para la máscara; el nuevo E2E de IN.9d para la fila final), verificados. Tablet (768 a 1.023px) sigue sin auditar. Desde IN.9b, el panel de actividad lee el ancho con `matchMedia` en cada render: un cambio de ancho sin cambio de estado no lo repinta, así que el límite y el reparto quedan en el del último render hasta la siguiente acción (misma limitación que `_repartoAccesosActividad()` heredó).

**Cambios realizados**

- 2026-08-02 IN.9e: `.hero-guia__escritorio` nueva (pieza centrada `max-width: 620px`, tres pasos numerados) convive en el DOM con `.hero-guia__movil` (intacta) dentro de `#hero-guia-saldo`; `responsive.css` decide cuál se ve por ancho (umbral 1024px), `render.js` sigue togglenando un solo `hidden`. Copy provisional (PI5). Cierra la iniciativa IN.9.
- 2026-08-02 CAL.5b: "Pendientes del mes" esconde lo ya pagado (`vencidosSinPagar`, defecto preexistente) y ofrece "Pagar los N" con dos o más pendientes, que abre el lote de Agenda sin navegar vía `lote:abrir`.
- 2026-08-01 IN.9d: `#panel-accesos-escritorio` (span 4, fila propia) y `#panel-actividad-reciente-escritorio` (span 6, junto a `#panel-resumen`) en `index.html`; `_repartoAccesosActividad()` nuevo en `render.js` alterna entre esos dos y `#accesos-actividad-movil` (la fusión, ahora acotada a menos de 1024px) según `_enEscritorio()`. `accesos/view.js` y `movimientos/view.js` llenan sus dos contenedores sin condicional. `#panel-resumen` pasa de `bento__cell--full` a `--half` y gana título propio (ya no depende del label de un grupo que ahora también tendría que describir Actividad). 9 tests unitarios + 2 E2E nuevos. SW v465 a v466.
- 2026-08-01 IN.9c (colateral del commit `ab8c9a1`, cierre documental el mismo día): `#panel-cuentas-detalle`/`#cuentas-detalle-lista` en `index.html`, columna propia de escritorio junto al hero desde 1024px. `updSaldo()` (render.js) reusa `_filasCuentas()` para pintar ahí; el acordeón (`#saldo-detalle`) queda acotado a menos de 1024px vía `_enEscritorio()`. Máscara de privacidad cubre total y las dos listas de detalle juntas. D3 del ADR 057, acota el 034 D4. Tests ya existían en el mismo commit (smoke.test.js, render.test.js). SW ya en v463, sin bump adicional.
- 2026-07-31 IN.9b: `_limiteRecientes()` en `movimientos/view.js` devuelve 5 bajo 1024px y 8 desde ahí; `movimientosRecientes()` ya recibía el límite por parámetro, así que no cambió la derivación ni el schema.
- 2026-07-31 IN.9a: `#panel-distribuir-inicio` y `#panel-limites` pasan a `bento__cell--half` y límites sube a la 2.ª posición del grupo, junto al otro aviso. Acota el ADR 034 D1 en las dos plataformas. Grupo "Atención hoy" a 1280px: 541px a 459px.
- 2026-07-31 BUG-021: la prueba E2E de "Pendientes del mes" fija el reloj con `page.clock.setFixedTime`; antes derivaba el día vencido de una envoltura modulo 28 y fallaba los días 1, 2, 3 y 31 del mes.

**Dónde vive**

| Pieza | Archivo | Ancla | Línea |
|---|---|---|---|
| Orquestador del dominio (suscripción a EventBus, re-render) | `modules/dominio/resumen/index.js` | `initResumen()` | ~16 |
| Resumen semanal (datos + serie diaria, IN.8f) | `modules/dominio/resumen/logic.js` | `resumenSemanal(gastos, hoyISO)`, `serieDiaria()` | ~202 |
| Resumen semanal (render visual: chip + barras + categoría top, IN.8f) | `modules/dominio/resumen/view.js` | `renderPanelResumen()` | ~51 |
| Estilos del bloque visual de resumen (namespace propio, no toca `.resumen-card__grid/__stat` que reusa "Me deben") | `styles/components/domain.css` | `.resumen-semana__*` | ~1569 |
| Hero de saldo (render + ojo/máscara) | `modules/infra/render.js` | `updSaldo()` | ~72 |
| Hero de saldo (estilos IN.8b: centrado, degradado, ojo absoluto) | `styles/components/domain.css` | `.hero-inicio`, `.hero-inicio__valor`, `.hero-inicio__ojo` | ~790 |
| Detalle por cuenta del hero (IN.8c: pill, filas, máscara; acotado a menos de 1024px desde IN.9c) | `modules/infra/render.js` / `index.html` | `alternarDetalleCuentas()`, `#saldo-detalle-toggle`, `#saldo-detalle`, acción `saldo-detalle` | |
| Detalle por cuenta en columna propia de escritorio (IN.9c, ADR 057 D3) | `modules/infra/render.js` / `index.html` | `updSaldo()`, `_enEscritorio()`, `#panel-cuentas-detalle`, `#cuentas-detalle-lista` | ~171 |
| Estado vacío del hero: guía móvil intacta + pieza de escritorio de tres pasos (IN.9e, ADR 057 D6/ID6) | `index.html` / `styles/layout.css` / `styles/responsive.css` | `#hero-guia-saldo`, `.hero-guia__movil`, `.hero-guia__escritorio`, `.hero-guia__pasos` | ~490 |
| Clase de entidad en infra (conteo del hero, espejo de claseEntidad) | `modules/infra/bancos.js` | `bancoClase(bancoId)` | |
| Excepción de padding móvil del hero | `styles/responsive.css` | `.hero-inicio` en `@media (max-width: 767px)` | |
| Saludo dinámico según hora, dos líneas + iniciales (IN.6a, IN.8d) | `modules/infra/render.js` | `updSaludo()`, `_iniciales()` | ~217 |
| Header de perfil en el DOM (IN.8d: avatar + saludo + ajustes) | `index.html` | `.perfil-inicio`, `#perfil-avatar`, `#saludo-franja`, `#saludo-inicio`, `#title-dash` (sr-only) | ~379 |
| Estilos del header de perfil | `styles/components/domain.css` | `.perfil-inicio*` | ~785 |
| Tokens del avatar (fijos en ambos temas, criterio ADR 025) | `styles/tokens.css` | `--fk-avatar-grad-a/-grad-b/-ink` | ~144 |
| Panel "Pendientes del mes" (compromisos vencidos, sin línea roja desde IN.8e) | `modules/dominio/compromisos/views/dashboard.js` | `renderPanelVencidos()` | ~35 |
| Fuente de vencidos del panel (CAL.5b: cruza el estado de pago del mes) | `modules/dominio/compromisos/logic/alertas.js` | `vencidosSinPagar(compromisos, gastos, hoyISO)` | |
| Vencidos por calendario puro (nudge de mora y alertas, sin cruce de pagos) | `modules/dominio/compromisos/logic/alertas.js` | `detectarVencidosCompletos(compromisos, hoyISO)` | ~200 |
| CTA "Pagar los N" del panel (CAL.5b, emite `lote:abrir`) | `modules/dominio/compromisos/views/dashboard.js` + `index.js` | `.vencidos-card__pagar`, acción `inicio-pagar-lote` | |
| Etiqueta de tipo por ítem (IV.2c, `.dom-badge`) en "Próximas prioridades" (sin cambios) | `modules/dominio/compromisos/views/dashboard.js` | `_tipoBadge()`, `_DOM_BADGE_POR_TIPO`, `_LABEL_POR_TIPO` | ~13 |
| Badge corto propio de "Pendientes del mes" (IN.8e: "Deuda"/"Gasto fijo", no la etiqueta larga de `_tipoBadge`) | `modules/dominio/compromisos/views/dashboard.js` | `_tipoBadgeCorto()` | ~48 |
| Estilos de "Pendientes del mes" (contador circular, meta, estado semántico) | `styles/components/domain.css` | `.vencidos-card__counter`, `.vencidos-card__meta`, `.vencidos-card__estado--danger/--warning` | ~1035 |
| Panel "Próximas prioridades" | `modules/dominio/compromisos/views/dashboard.js` | `renderPanelPrioridades()` | ~106 |
| Fuente 1 de prioridades (compromisos) | `modules/dominio/compromisos/logic.js` | `compromisosProximos(comps, 7)` | ~32 |
| Fuente 2 de prioridades (préstamos personales) | `modules/dominio/compromisos/views/dashboard.js` | `_personalesProximos(7)` | ~218 |
| Fuente 3 de prioridades (apartados) | `modules/dominio/compromisos/views/dashboard.js` | `_apartadosProximos(7)` | ~239 |
| Combinación manual de las 3 fuentes | `modules/dominio/compromisos/views/dashboard.js` | `[...proxComp, ...proxPers, ...proxApar].sort(...)` | ~122 |
| Aviso de distribuir ingreso desde Calendario (ADR 021, se conserva) | `modules/dominio/agenda/index.js` | `EventBus.emit('distribuir:abrir')` | ~61 |
| Nudge de distribuir ingreso en Inicio (CAL.1) | `modules/dominio/tesoreria/views/distribucion.js` | `renderNudgeDistribucionInicio()` | ~45 |
| Estado del nudge (mismo guard que Mis cuentas, sin marcador nuevo) | `modules/dominio/tesoreria/logic/distribucion.js` | `estadoDistribucion(ingresos, ultimaDistribucionPeriodo, hoy)` | ~137 |
| CTA del nudge (emite `distribuir:abrir`) | `modules/dominio/tesoreria/acciones/distribucion.js` | `_distribuirDesdeInicio()`, `data-action="distribuir-desde-inicio"` | ~127 |
| Elemento del nudge en el DOM | `index.html` | `#panel-distribuir-inicio` | ~437 |
| Receptor del aviso, abre el asistente en Tesorería (ambos orígenes) | `modules/dominio/tesoreria/index.js` | `EventBus.on('distribuir:abrir', ...)` | ~68 |
| Perfil del usuario (nombre, no renderizado en Inicio hoy) | `modules/core/state.js` | `S.perfil = { nombre, smmlv }` | ~272 |
| Config de usuario (accesos personalizados desde IN.4a, sin avatar todavía) | `modules/core/state.js` | `S.config = { notificaciones, perfilFiscal, datosFiscales, accesosInicio }` | ~278 |
| Preferencia de ocultar saldo (agregada dinámica, sin bump de schema) | `modules/infra/render.js` | lectura defensiva `S.config?.ocultarSaldo` | ~76 |
| Catálogo de categorías de gasto (fijo, 19 + 2 internas) | `modules/core/constants.js` | `CATEGORIAS_GASTO` | ~400 |
| Categorías visibles al usuario | `modules/core/constants.js` | `CATEGORIAS_GASTO_USUARIO` | ~426 |
| Mapeo categoría → símbolo del sprite | `modules/core/constants.js` | `CATEGORIA_ICONO` | ~448 |
| Eventos de compromisos por mes (patrón de referencia, no cruza dominios) | `modules/dominio/agenda/logic.js` | `eventosDelMes(compromisos, year, month)` | ~104 |
| Eventos de ingresos por mes | `modules/dominio/agenda/logic.js` | `eventosIngresosDelMes(ingresos, year, month)` | ~144 |
| Dominio nuevo: derivación de movimientos (TX.8a) | `modules/dominio/movimientos/logic.js` | `movimientosDesdeGastos()`, `movimientosDesdeIngresosPuntuales()`, `movimientosDesdeAportes()`, `movimientosRecientes()` | |
| Panel "Actividad reciente" (render, header simplificado desde IN.8g) | `modules/dominio/movimientos/view.js` | `renderActividadReciente()` | ~40 |
| Orquestador de movimientos (registrarRender, state:change) | `modules/dominio/movimientos/index.js` | `initMovimientos()` | |
| Fusión con Accesos rápidos en un solo bento__cell, acotada a móvil desde IN.9d (IN.8g, ADR 034 D7 + ADR 057 D4) | `index.html` / `styles/components/domain.css` | `#accesos-actividad-movil`, `.accesos-actividad__seccion(--actividad)`, `.accesos-actividad__header`, `.accesos-actividad__label` | ~494 |
| Elemento del panel de actividad en el DOM (dentro del bloque fusionado, solo móvil) | `index.html` | `#panel-actividad-reciente` | ~500 |
| Accesos rápidos y Actividad reciente en escritorio, y el reparto que decide cuál se ve (IN.9d, ADR 057 D4) | `index.html` / `modules/infra/render.js` / `modules/dominio/accesos/view.js` / `modules/dominio/movimientos/view.js` | `#panel-accesos-escritorio`, `#panel-actividad-reciente-escritorio`, `_repartoAccesosActividad()` | ~223 |
| Grupos "Atención hoy" / "Resumen de la semana" (IN.8a, ADR 034 D1) | `index.html` / `styles/layout.css` | `.bento__group`, `.bento__group-label`, `.bento__group-cells` | ~424 |
| Celda plana de los paneles dinámicos (mata el doble contenedor) | `styles/utils.css` | `.bento__cell--flat` | final |
| Aire móvil del dashboard (20px bloques / 12px intra-grupo) | `styles/responsive.css` | `.bento--dash`, `.bento__group-cells` en `@media (max-width: 767px)` | |
| Ícono para gastos con categoría interna "Gastos fijos" (nuevo) | `modules/core/constants.js` | `CATEGORIA_ICONO['Gastos fijos'] = 'i-recurring'` | ~466 |
| Campo `dominio` del Movimiento, colorea la teja por sección (TX.8b) | `modules/dominio/movimientos/logic.js` | `_CATEGORIAS_COMPROMISOS`, campo `dominio` en las 3 funciones `movimientosDesde*` | |
| Historial completo sin límite (TX.8b) | `modules/dominio/movimientos/logic.js` | `movimientosCompletos()` | |
| Vista completa de Movimientos, ruta propia (TX.8b), paginada por lotes (PERF.1) | `modules/dominio/movimientos/view.js` | `renderMovimientosCompletos()`, `cargarMasMovimientos()`, `_agregarSiguienteLote()`, `_agruparPorMes()`, `_aplanarEntradas()`, `_renderMovimientoItem()` | |
| Acción del botón "Cargar más" (PERF.1) | `modules/dominio/movimientos/index.js` | `data-action="movimientos-cargar-mas"` | |
| Memoización de `movimientosRecientes()`/`movimientosCompletos()` (PERF.2) | `modules/dominio/movimientos/view.js` | `_movimientosRecientesMemo`, `_movimientosCompletosMemo`, `_extraerFuentes` | |
| Helper genérico de memoización (PERF.2, nuevo, compartido con `resumen/` y `analisis/`) | `modules/infra/memo.js` | `memoizar()` | |
| Sección y ruta `#movimientos` (sin ícono fijo en la barra) | `index.html` / `modules/infra/router.js` | `#sec-movimientos` / `SECTIONS.get('movimientos')` | |
| Link "Ver todo" del panel compacto hacia la vista completa | `modules/dominio/movimientos/view.js` | `.actividad-reciente__ver-todo`, `href="#movimientos"` | |
| Gastos deja de listar categorías internas (TX.8b) | `modules/dominio/gastos/view.js` | `_CATEGORIAS_INTERNAS`, `_sinInternas()` (usado en `renderListaGastos()` y `renderFiltrosGastos()`) | |
| Dominio nuevo: accesos rápidos personalizables (IN.4a) | `modules/dominio/accesos/logic.js` | `accesosVisibles(ids, catalogo)`, `alternarAcceso(ids, id)` | |
| Catálogo de secciones elegibles (8: Mis cuentas, Deudas, Ahorros, Límites, Me deben, Análisis, Movimientos, Ajustes) | `modules/core/constants.js` | `ACCESOS_INICIO`, `ACCESOS_INICIO_DEFAULT` | ~706 |
| Preferencia del usuario (bump v23) | `modules/core/state.js` / `modules/core/storage.js` | `S.config.accesosInicio` / migración v22→v23 | |
| Fila de tiles bajo el hero (reusa `.menu-mas__item`, ya coloreado por dominio) | `modules/dominio/accesos/view.js` | `renderAccesosInicio()` | |
| Modal "Personalizar" (lista completa, toggle instantáneo, sin drag & drop) | `modules/dominio/accesos/view.js` / `index.js` | `renderModalPersonalizarAccesos()` / acciones `accesos-personalizar`, `accesos-toggle` | |
| Elemento de los tiles y el modal en el DOM | `index.html` | `#accesos-inicio-grid`, `#modal-personalizar-accesos` | ~437, ~824 |

**Recursos**: sprite `i-saldo` para el hero; tokens `--fk-*` del bento grid (no auditados en detalle en este pase). El hero ocupa ancho completo (`bento__cell--full`) desde IN.5.

**Dependencias y relaciones**: `resumen` y `movimientos` escuchan `EventBus` para re-renderizar al cambiar sus fuentes (ADN 10 respetado: ningún dominio importa a otro; `movimientos/logic.js` no lee `S` ni importa `gastos/`, `tesoreria/` o `ahorro/`, recibe los arrays ya extraídos de `view.js`). El aviso de distribución de ingreso puede nacer en `agenda` (Calendario, ADR 021) o directo desde el nudge de `tesoreria` en Inicio (CAL.1); ambos emiten el mismo `distribuir:abrir` y lo resuelve `tesoreria`, que registra su propio render del nudge vía `registrarRender()` (mismo patrón que `compromisos` y ahora `movimientos` usan para sus paneles del dashboard).

**Riesgos**:

- ~~**Discrepancia con el brief del usuario**~~: **resuelto (IN.4a)**. El código real solo tenía **1** acceso rápido fijo (Gasto rápido); el catálogo `ACCESOS_INICIO` y los 3 tiles personalizables (default: Mis cuentas, Ahorros, Límites de gasto, elegidos por análisis de frecuencia de autoconsulta, no por preferencia personal) parten de ese hecho verificado. **Gasto rápido se eliminó después (IN.5, 2026-07-06)** junto con su panel "Gastos por organizar"; el hero pasó a ancho completo. Los accesos personalizables de IN.4a son ahora el único bloque de atajos del dashboard.
- ~~**Overlap confirmado entre paneles** (base de **IN.7**)~~: **resuelto**. Un compromiso con `diaPago = hoy` aparecía a la vez en "Pendientes del mes" (`renderPanelVencidos`, vía `detectarVencidosCompletos`) y en "Próximas prioridades" (`renderPanelPrioridades`, vía `compromisosProximos` con `diasRestantes = 0`). `renderPanelPrioridades()` ahora filtra `diasRestantes > 0` sobre los compromisos antes de combinarlos con personales/apartados: lo que vence hoy vive solo en Pendientes del mes. `compromisosProximos()` (logic) no se tocó: otros consumidores (nudge de mora, `nivelAlertaMora`) siguen necesitando el día 0.
- **Categorías son catálogo cerrado**: `CATEGORIAS_GASTO`/`CATEGORIA_ICONO` no soportan categorías creadas por el usuario hoy. Relevante para **TX.9** (categoría personalizada) y **TX.10** (categoría como eje de automatización): requiere diseño de dato nuevo + migración de schema (ADN 6), no solo UI.
- ~~**Sin helper cross-dominio en `infra/`**~~: **resuelto (TX.8a)**. Se creó el dominio nuevo `movimientos/` como agregador (no un helper en `infra/`, que no puede leer `S`): su `logic.js` lee los arrays ya normalizados que le pasa `view.js` (que sí lee `S` directo), sin importar `gastos/`, `tesoreria/` ni `ahorro/`. Respeta ADN 10 igual que el patrón de `resumen/`.
- **Íconos de "Actividad reciente"/Movimientos no heredan el origen exacto del compromiso** (a diferencia de la lista de Gastos, que sí usa `iconoPorOrigen()` de TX.6/TX.7): un abono a deuda o pago de fijo muestra el ícono de su categoría interna (`i-deudas`, `i-recurring`), no el ícono específico del compromiso (ej. la categoría real del fijo en Agenda). Se decidió así para no importar `gastos/logic.js` desde `movimientos/` (ADN 10); revisar si vale la pena mover `iconoPorOrigen()` a `infra/` si la diferencia visual molesta en la práctica. TX.8b sí distingue el **dominio** (color de la teja: `compromisos` vs `gastos`), pero no el ícono exacto.
- **`S.config.ocultarSaldo` se agregó sin bump de versión de schema** (lectura defensiva con `?.`), a diferencia de `accesosInicio` (IN.4a), que sí entró formalmente al bump v23 con migración idempotente. Queda como el único campo opcional de `S.config` sin migración propia; revisar si vale la pena formalizarlo cuando se toque `S.config` de nuevo (ej. `perfil.avatar` en IN.6b).
- ~~**`S.perfil.nombre` ya existe pero no se renderiza en Inicio**~~: **resuelto (IN.6a)**. `updSaludo()` lo consume directo, sin dato nuevo.

**Cambios pendientes**: ninguno de la iniciativa "Inicio v2" ([ADR 034](../DECISIONS/034-inicio-v2.md)): las 7 rebanadas IN.8a a IN.8g cerraron el 2026-07-12. Quedan las validaciones formales de Esteban descritas al final del ADR (fotografía de perfil, aprobación completa P2-P5 del ADR 033) como trabajo futuro opcional, sin tarjeta propia hoy. **Nota de alcance de IN.8e:** el objetivo del ADR D5 también menciona "Próxima prioridad como tarjeta única destacada cuando son pocas" para `renderPanelPrioridades()`; la tarea que cerró esta rebanada nombró explícitamente solo "Pendientes del mes + Gestionar → Calendario", así que `renderPanelPrioridades()` no se tocó (sigue con su estructura agrupada por día de siempre). Queda como detalle a validar con Esteban: si la lista ya se ve "como una sola tarjeta" cuando hay pocos ítems (comportamiento natural, sin código nuevo) o si el ADR pedía una variante visual explícita distinta a la actual. IN.6b quedó resuelta dentro del ADR 034 D8 (iniciales; set ilustrado opcional a futuro). Dato técnico ya verificado para IN.8f: `resumenSemanal()` NO expone serie diaria (solo agregados), el cálculo nuevo va en `resumen/logic.js` dentro del bundle memoizado existente. Metas y apartados siguen sin aparecer en el historial (sin registros fechados por aporte); la categoría interna `'Ahorro'` existe en `CATEGORIAS_GASTO` pero ningún flujo crea gastos con ella todavía (verificado de nuevo al cerrar TX.8b, sigue igual). "Movimientos" no tiene ícono propio en la barra de navegación ni en el menú "Más" (se llega por el link "Ver todo", por hash directo, o ahora también por su propio tile si el usuario lo personaliza en IN.4a).

**Cambios realizados**:

- 2026-07-25 (V1, [ADR 054](../DECISIONS/054-el-acento-no-mide-gasto.md)): el mini gráfico semanal deja de medir gasto con el acento de marca. Barras a `--fk-dom-gastos` 28%, pico al 100%, etiqueta del pico a `--fk-dom-gastos-text` (en claro baja a `#d13b00`, 4.84:1 sobre blanco). El chip "12% menos" sigue verde (ADR 019: bajar el gasto es logro). Revisa el ADR 034 D6 en vez de corregirlo en silencio. Solo CSS (`.resumen-semana__barra-fill`, `--pico`, `.resumen-semana__dia--pico`). Las 6 reglas R1 a R6 de la auditoría y la del radio protagonista quedaron escritas en `DESIGN_SYSTEM.md`. 3007/3007 unit verdes. SW v416 → v417.
- 2026-07-12 (IN.8g, ADR 034 D7, **cierra la iniciativa "Inicio v2"**): fusión de "Accesos rápidos" + "Actividad reciente" en un solo `bento__cell` (`.accesos-actividad`), último bloque de la pantalla. Arriba: header con label "Accesos rápidos" + botón "Personalizar" en la misma fila (antes el botón vivía debajo de la grilla) + la grilla de tiles sin cambios (`renderAccesosInicio()` intacto). Abajo, separado por `border-top` (`.accesos-actividad__seccion--actividad`, que desaparece solo junto con el panel cuando no hay movimientos, sin lógica nueva): header con label "Actividad reciente" + link "Ver todo" en la misma fila (antes: encabezado con ícono propio arriba, "Ver todo" como pie de página abajo) + la lista de movimientos sin cambios (`movimientosRecientes()` intacto). Cero cambios en `accesos/logic.js` y `movimientos/logic.js` (regla del ADR). CSS: `.accesos-actividad` toma la superficie de card que antes tenían por separado el `bento__cell` de accesos y `.actividad-reciente`; nuevas `.accesos-actividad__seccion/__header/__label` (label en `--fk-text-secondary`, no `--fk-text-muted`: mismo criterio ya documentado en `.bento__group-label` sobre por qué muted falla AA fuera de `bg-base`); `.actividad-reciente__ver-todo` pasa de link de pie de página a link inline junto al label (mismo patrón que `.vencidos-card__link`). 2 tests unitarios nuevos + 1 E2E. 2367/2367 unit + 169/169 E2E + lint verdes. SW v353 → v354.
- 2026-07-12 (IN.8f, ADR 034 D6): resumen semanal como bloque visual. `resumenSemanal()` se extiende de forma aditiva (ningún campo existente cambia) con `serie` (7 días, más antiguo a más reciente, `serieDiaria()` nuevo), `diasActivosSemana` (días de la ventana con gasto > 0, distinto de `diasActivos` mensual que se conserva) y `diaPico` (nombre completo del día con más gasto, `null` si la semana no tuvo gasto). `renderPanelResumen()` reemplaza el grid plano de stats por un bloque bespoke (`.resumen-semana__*`, namespace propio: `.resumen-card__grid/__stat/__label/__value` NO se tocan porque "Me deben" los reusa): monto grande (`--fk-text-3xl`, 30px, más cerca de los 28px del diseño que `--fk-text-2xl`) + chip comparativo compacto ("12% menos" en verde `--fk-success-bg/-text` solo cuando el gasto bajó; "12% más"/"igual"/"sin previa" comparten un tono neutro, mismo criterio que IV.3: nunca rojo, ADR 019) + mini gráfico de 7 barras (`--fk-accent` al 100% en el día pico, ~28% de opacidad en el resto, alto proporcional tope 72% del contenedor de `--fk-space-20` (80px, 2px más que el mockup, evita un valor fuera de la escala) + etiquetas de día resaltadas en el pico + fila de categoría top con `tejaCategoria()` (`--fk-teja-lg`, 36px) y mensaje interpretativo ("Mercado fue tu categoría top · 5 de 7 días activos · mayor gasto el sábado"). Copy del label ajustado a "Gastaste esta semana" (antes "Gastaste estos 7 días") para calzar con el texto del diseño hifi. El viejo stat "Constancia" (días activos del mes) se retira de este panel (Análisis ya cubre el detalle mensual, ADR 028 D5); `.resumen-card__trend*` quedó sin consumidor tras el cambio y se eliminó (CSS muerto). 11 tests unitarios nuevos (`serieDiaria()`, campos nuevos de `resumenSemanal()`, markup del chip/barras/categoría top) + 1 E2E. 2366/2366 unit + 168/168 E2E + lint verdes. SW v352 → v353.
- 2026-07-12 (IN.8e, ADR 034 D5): "Pendientes del mes" con jerarquía real, sin línea roja. `vencidos-card` pierde el `border-left: 3px solid var(--fk-danger)` (ADR 019, "gastar no es incumplir"): la urgencia vive SOLO en un nuevo texto de estado por ítem (`.vencidos-card__estado--danger/--warning`), nunca en el borde/fondo de la tarjeta. Cada ítem gana una fila `.vencidos-card__meta` con badge corto (`_tipoBadgeCorto()` nuevo: "Deuda"/"Gasto fijo", más corto que `_tipoBadge()` de "Próximas prioridades", que no cambia) + estado temporal semántico ("Venció hace N días"/"Venció ayer" en danger, "Vence hoy" en warning, ambos tokens ya validados AA en el resto de la app). El título deja de incluir el conteo ("2 pendientes del mes" → "Pendientes del mes" + badge circular `.vencidos-card__counter` con el número, `--fk-danger-bg`/`-text`). Monto del ítem sube a peso `--fk-font-bold` + `tabular-nums` (15px del ADR sin token exacto: se conserva `--fk-text-sm`, mismo criterio de IN.8b). **"Gestionar" pasa de `#compromisos` a `#agenda`** (ampliación del 3.er lote, 2026-07-08, formalizada en el ADR). 7 tests unitarios nuevos (`compromisos.test.js`) + 1 E2E (badge, estado, colores, link). 2356/2356 unit + 167/167 E2E + lint verdes. SW v351 → v352.
- 2026-07-12 (IN.8d, ADR 034 D8): header de perfil del dashboard. El header de `#sec-dash` pasa de título + subtítulo a `.perfil-inicio`: teja de iniciales 46px/radio 14 con gradiente del acento (tokens nuevos `--fk-avatar-grad-a/-grad-b/-ink`, colores FIJOS en ambos temas por el criterio de tejas de marca del ADR 025; tinta oscura 9.6:1 sobre la parada más clara) + saludo en dos líneas (`#saludo-franja` 14px secundario arriba, `#saludo-inicio` 20px/700 abajo, medidas ajustadas a la escala de tokens igual que hizo IN.8b con el hero) + enlace de ajustes 40px a `#config` (`.perfil-inicio__ajustes`, ícono `i-ajustes` existente). `updSaludo()` (render.js) reparte franja/nombre/iniciales; `_iniciales()` nuevo toma la primera letra de las dos primeras palabras con spread (no parte caracteres de dos unidades). Fallback sin nombre: la franja saluda sola (sin coma) y nombre y avatar se ocultan (sin dato nuevo, decisión de la rebanada prevista en el ADR). Encabezado accesible resuelto: el h1 "Tu resumen" queda `sr-only` y sigue nombrando la región vía `aria-labelledby` (pase axe del suite unitario verde con el marcado nuevo). El nombre visible es el título de la pantalla; la lógica de franjas horarias no cambió. 2 tests netos nuevos en render.test.js (fixture réplica del header + iniciales 1/2 palabras + fallbacks). 2349/2349 unit + 166/166 E2E + lint verdes. SW v350 → v351.
- 2026-07-12 (IN.8c, ADR 034 D4): detalle por cuenta expandible en el hero. Pill "Ver detalle por cuenta" ↔ "Ocultar detalle" bajo el monto (`#saldo-detalle-toggle`, acción nueva `saldo-detalle` en actions.js) que expande in situ una fila por cuenta activa: teja de marca (`bancoAvatar()`, ADR 025) + nombre escapado + saldo tabular. Estado SOLO de UI en memoria (`_detalleCuentasAbierto` en render.js, sin tocar S.config: verificado en test), colapsado por defecto en cada apertura (decisión del ADR). La máscara del ojo cubre total Y detalle juntos (extensión IN.2): filas con `SALDO_MASCARA_CUENTA` ('••••') y ningún saldo real toca el DOM oculto. El conteo "efectivo + N cuentas bancarias" ahora es real (`_descCuentas()`: singular/plural, sin efectivo, solo efectivo) y se muestra únicamente colapsado. `bancoClase()` nuevo en infra/bancos.js (espejo de `claseEntidad()` de tesorería: infra no importa dominios, ADN 10). Animación de entrada 180ms solo opacity+transform (ADR 033 D4) con `prefers-reduced-motion`. 10 tests unitarios nuevos (render.test.js) + 1 E2E (expandir, máscara, no persistencia tras recarga). 2347/2347 unit + 166/166 E2E + lint verdes. SW v349 → v350.
- 2026-07-12 (IN.8b, ADR 034 D2/D3 + piloto ADR 033 D1/D2): hero con saldo protagonista. Marcado nuevo en `index.html` (`.hero-inicio`): se elimina el ícono decorativo `#hero-saldo-icon` (y su manejo en `updSaldo()`), label en sentence case (14px secundario) + monto centrado (objetivo 42px/800, `clamp()` solo protege < 390px; tabular-nums, letter-spacing -0.02em, color primario: la identidad la pone el degradado, no un número verde) + descripción 12px secundario. Ojo de privacidad reescrito como botón propio (sin `.btn`, para que el min-height táctil de 44px de responsive.css no deforme los 40px del diseño) con `position:absolute` en la esquina superior derecha: su posición ya no depende del ancho del monto. Verificado en Chromium real midiendo el `boundingBox` en reposo en los 3 estados (visible → oculto → visible): posición idéntica al píxel; el corrimiento de 2px que apareció al medir era el lift de hover de `.bento__cell` (comportamiento preexistente por diseño), documentado en el E2E nuevo. Fondo del piloto ADR 033: `linear-gradient(160deg, accent 14% → transparent 55%)` sobre surface + borde `--fk-accent-border` + `--fk-shadow-md`; ambos temas heredan por tokens (sin cambios en themes.css). Contraste WCAG medido contra la parada fuerte: oscuro primario 11.16:1 / secundario 5.67:1; claro 14.62:1 / 7.40:1; el texto muted NO se usa dentro del hero (4.13:1 en oscuro, falla AA), documentado en el propio CSS. El contrato de `updSaldo()`/`S.config.ocultarSaldo` no cambió (D3). 1 E2E nuevo (posición del ojo estable al alternar). 2337/2337 unit + 165/165 E2E + lint verdes. SW v348 → v349.
- 2026-07-12 (IN.8a, ADR 034 D1): reorden del dashboard. Orden nuevo del `.bento--dash` en `index.html`: hero → grupo "Atención hoy" (nudge de distribuir + Pendientes del mes + Próximas prioridades + alertas de límites) → grupo "Resumen de la semana" → accesos rápidos → actividad reciente. Los grupos usan un contenedor nuevo `.bento__group` (label `.bento__group-label` 12px/700 uppercase muted + grilla interna `.bento__group-cells` que espeja las columnas del bento: 12 desktop / 6 tablet / 1 móvil); un grupo sin celdas visibles desaparece completo vía `:has()` (layout.css). Los paneles dinámicos ganan `.bento__cell--flat` (utils.css, capa alta a propósito: debe ganarle al padding responsive de `.bento__cell` y al `margin-bottom` de las cards internas): el componente interno pone su propia superficie y muere el doble contenedor card-dentro-de-card que existía desde TX.8a. Aire en móvil: 20px entre bloques (`.bento--dash`), 12px dentro de los grupos (responsive.css). `renderPanelResumen()` ya no repite el encabezado interno "Resumen de la semana" (el label del grupo es el título; el rediseño completo de la card llega en IN.8f). Medido en Chromium real: label→cards 12px, card→card 12px, bloque→bloque 20px. Ningún dominio cambió su lógica. 2337/2337 unit + 164/164 E2E + lint verdes. SW v347 → v348.
- 2026-07-12 (IN.8 fase de análisis, sin cambios de código): Esteban entregó el handoff de diseño hifi de Inicio v2 (`Iteración de specimen/design_handoff_inicio_v2/`: mockup interactivo + decisiones D1-D8, tokens tomados del propio repo) y autorizó implementarlo con las opciones recomendadas. Se escribió el [ADR 034](../DECISIONS/034-inicio-v2.md) (revisión formal del ADR 028), se actualizó el estado del [ADR 033](../DECISIONS/033-direccion-visual-premium.md) a "Propuesta con estreno parcial autorizado" (D1 sombra en reposo + D2 degradado de identidad, acotados a Inicio) y la iniciativa quedó re-cortada en IN.8a a IN.8g en el BOARD. Verificaciones de código hechas en esta fase: `resumenSemanal()` sin serie diaria (confirma cálculo nuevo para IN.8f), "Gestionar" apunta a `#compromisos` (IN.8e lo pasa a `#agenda`), tokens `--fk-shadow-*` existentes y `--fk-grad-identity` inexistente (lo estrena IN.8b sobre `--fk-accent`), ojo del hero en flujo (causa real del desplazamiento que IN.8b corrige con posición absoluta).
- 2026-07-10 (IV.2c, ADR 031): "Pendientes del mes" y "Próximas prioridades" ganan una etiqueta de texto con el tipo del ítem (`.dom-badge`, componente reutilizado de IV.2d), cumpliendo la regla D1 "el color nunca viaja solo". Mapeo local en `dashboard.js` (`_DOM_BADGE_POR_TIPO`/`_LABEL_POR_TIPO`): reusa `LABEL_TIPO` de `compromisos/logic.js` para fijo/deuda-entidad/deuda-personal y agrega 'personal' (Me deben, "Préstamo") y 'apartado' ("Apartado"), que no viven en ese catálogo. **Bug real corregido de paso**: `dotTipo` de un ítem `apartado` en "Próximas prioridades" caía a `'fijo'` (prestaba el color índigo de gasto fijo en vez del suyo); ahora usa `cal-dot--apartado` (nuevo, familia menta de Ahorro). `vencidos-card__icon--fijo`/`.cal-dot--fijo` pasan de `--fk-dom-presupuesto` a `--fk-dom-agenda` (mismo criterio del calendario, IV.2c). Sin bump de schema, sin cambios de datos. 2295/2295 unit + 159/159 E2E verdes. SW v343 → v344.
- 2026-07-06 (PERF.2, auditoría de rendimiento): `resumenSemanal()` y `movimientosRecientes()`/`movimientosCompletos()` recalculaban sobre todo `S.gastos` (y las otras 2 fuentes de Movimientos) en cada `state:change` relevante, incluso en re-renders redundantes (`renderAll()` repintando el dashboard sin que esos datos cambiaran, o dos listeners reaccionando a la misma acción). Ambas se envolvieron con `memoizar()` (nuevo, `infra/memo.js`, compartido con Análisis): cachea contra la identidad de los arrays de entrada + un contador de revisión por sección alimentado por `EventBus`, sin Proxies sobre `S` (ADN 4). Para Movimientos, que pasa un objeto envoltorio nuevo en cada llamada (`{ gastos, ingresosPuntuales, aportes, categoriasPersonalizadas }`), se usó un `extraerClave` que compara los arrays de adentro, no el envoltorio. Ninguna de las dos funciones cambia su HTML resultante: el cacheo es solo de cómputo, el DOM se reescribe siempre. Medido con `pnpm perf`: en la llamada "fría" (cache miss forzado) el costo no cambia respecto a PERF.1; en la llamada "caché" (repetición sin cambios), Inicio pasa de ~4-97 ms a ~2-17 ms. 10 tests nuevos en `tests/unit/memo.test.js`. 2219/2219 unit + 151/151 E2E verdes. SW v331 → v332. Detalle completo en [`scripts/perf/BASELINE.md`](../../scripts/perf/BASELINE.md) y [`contexto/analisis.md`](analisis.md) (para la parte de Análisis).

- 2026-07-06 (PERF.1, auditoría de rendimiento): la vista completa de Movimientos (`#movimientos`) pintaba todo el historial de una sola vez (~3,9 s con 10 años de datos simulados, medido en `scripts/perf/bench.perf.js`). `renderMovimientosCompletos()` ahora pinta un primer lote de 50 movimientos (el conteo cuenta solo ítems reales; un divisor de mes no resta cupo, vía `_aplanarEntradas()`) y agrega el resto bajo demanda con `cargarMasMovimientos()` (nuevo, exportado), disparada por el botón accesible `data-action="movimientos-cargar-mas"` ("Cargar más movimientos", 100% operable por teclado) y, como mejora progresiva, por un `IntersectionObserver` sobre ese mismo botón (auto-carga al hacer scroll hasta él; se descarta y recrea en cada lote, nunca hay más de uno vivo). El panel compacto de Inicio (`renderActividadReciente()`, límite fijo de 5) no cambia. CSS nuevo mínimo: `.movimientos-cargar-mas` en `domain.css`. 8 tests nuevos en `movimientos.test.js` (primer lote acotado a 50, control "Cargar más" aparece/desaparece, `cargarMasMovimientos()` no duplica ni repite divisores de mes al cortar a mitad de mes, reiniciar el render no acumula lotes viejos). 2209/2209 unit + 151/151 E2E verdes en Chromium real (Playwright). SW v330 → v331. Detalle de la medición (antes/después, hasta 81x más rápido a 10.000 gastos) en [`scripts/perf/BASELINE.md`](../../scripts/perf/BASELINE.md).
- 2026-07-05 (IN.4a): accesos rápidos personalizables. Catálogo `ACCESOS_INICIO` (8 secciones elegibles: Mis cuentas, Deudas, Ahorros, Límites de gasto, Me deben, Análisis, Movimientos, Ajustes) y `ACCESOS_INICIO_DEFAULT` en `constants.js`; bump de schema v23 (`S.config.accesosInicio`, migración idempotente). Default elegido con análisis objetivo de frecuencia de autoconsulta y qué ya cubre Inicio hoy (no preferencia personal, a pedido explícito de Esteban): **Mis cuentas, Ahorros, Límites de gasto** (triangula con el patrón de Mint/YNAB/Fintonic: cuentas + presupuesto + metas como pantalla principal). Dominio nuevo `modules/dominio/accesos/` (`logic.js` puro: `accesosVisibles()`, `alternarAcceso()`; `view.js`: `renderAccesosInicio()` para la fila de tiles y `renderModalPersonalizarAccesos()` para el modal; `index.js`: acciones `accesos-personalizar`/`accesos-toggle`). Los tiles reusan `.menu-mas__item`/`__icon`/`__label` tal cual (mismo lenguaje visual y coloreado por dominio que el menú "Más", cero CSS nuevo de color); el modal reusa `.list-item`/`tejaCategoria()` (mismo patrón que Movimientos/Gastos). Personalización v1 por lista con toggle instantáneo (tocar agrega/quita, sin drag & drop, ADR 028 D2); el botón "Personalizar" queda visible aunque el usuario quite los 3 tiles. Bug encontrado y corregido en el propio desarrollo: el `<p>` instructivo del modal usaba la clase `.confirm__mensaje`, que ya identificaba de forma única al mensaje de un modal de confirmación activo en los tests E2E existentes (`smoke.test.js`); al vivir siempre en el DOM (no solo cuando el modal está abierto), rompía ese selector con un "strict mode violation". Se corrigió reusando `.form-hint` (helper genérico ya existente), sin tocar el contrato de `.confirm__mensaje`. Se agregó `movimientos/` y `accesos/` al precache de `service-worker.js`. 20 tests nuevos (`accesos.test.js`: lógica pura, render de tiles y modal, toggle end-to-end vía `dispatch()`; `storage.test.js`: migración v22→v23). 2188/2188 unit + 89/89 E2E (smoke + navegación) verdes en navegador real (Playwright); verificado además con un flujo manual Dashboard → tiles default → Personalizar → toggle → persistencia tras navegar → tap-through a la sección real. SW v326 → v327.
- 2026-07-05 (TX.8b): vista completa de Movimientos en ruta propia `#movimientos` (sin ícono fijo en la barra), agrupada por mes ("Julio 2026"), sin totales (el resumen financiero es de Análisis, ADR 028 D5). Cada `Movimiento` ahora lleva un campo `dominio` (`gastos`/`compromisos`/`ingresos`/`ahorro`, según tipo y categoría interna) que colorea la teja del ícono con `tejaCategoria()` (mismo patrón que Gastos/Deudas). `movimientosCompletos()` nuevo en `logic.js` (reusa `movimientosRecientes()` con límite `Infinity`, sin duplicar la combinación/orden). El panel compacto de Inicio suma un link "Ver todo" hacia la vista completa. En paralelo, `renderListaGastos()`/`renderFiltrosGastos()` excluyen las categorías internas ('Deudas', 'Gastos fijos') vía `_sinInternas()`: Gastos queda enfocada en gasto cotidiano; `S.gastos` no se toca, así que Análisis y Límites (que leen todo el historial) no se ven afectados. Se corrigió además un hueco de TX.8a: `movimientos/{logic,view,index}.js` no estaban en el precache de `service-worker.js` (`CORE_ASSETS`), ahora sí. 17 tests nuevos en `movimientos.test.js` (dominio por fuente, `movimientosCompletos()`, `renderMovimientosCompletos()`: lista completa, cuenta, agrupación por mes, teja por dominio, "Ver todo") + 7 en `gastos.test.js` (exclusión de internas en lista/chips/total) + 1 E2E de regresión hashchange (`navegacion-render.test.js`, ruta sin ícono de nav). 2168/2168 unit + E2E smoke/navegación verdes en navegador real (Playwright); verificado además con un flujo manual Dashboard → "Ver todo" → Movimientos con datos reales. SW v325 → v326.
- 2026-07-05 (TX.8a): dominio nuevo `modules/dominio/movimientos/` (`logic.js`, `view.js`, `index.js`) que deriva un historial unificado desde `S.gastos`, `S.ingresosPuntuales` y `S.ahorro.aportes` (sin log paralelo, ADR 028 D5). Panel "Actividad reciente" en Inicio con los últimos 5 movimientos, ordenados por fecha descendente. Se agregó `CATEGORIA_ICONO['Gastos fijos'] = 'i-recurring'` en `constants.js` para que los pagos de fijos (categoría interna) tengan ícono propio en vez de caer al genérico `i-gastos`; `'Deudas'` y `'Ahorro'` ya tenían íconos. Reutiliza `tiempoRelativo()`/`f()`/`icon()` ya existentes en `infra/`; CSS nuevo `.actividad-reciente*` en `domain.css` siguiendo el mismo patrón visual que `vencidos-card`/`prioridades-card`. 26 tests nuevos (`movimientos.test.js`): normalización de las 3 fuentes, orden y límite, render oculto/visible/límite de 5. E2E smoke 82/82 verde (nuevo dominio carga sin errores en navegador real).
- 2026-07-05 (CAL.1): nudge de "Distribuir mi ingreso" en Inicio, bloque "Atención hoy". Reutiliza `estadoDistribucion()` y `S.config.ultimaDistribucionPeriodo` **ya existentes** (el panel equivalente de Mis cuentas ya los usaba): no hizo falta marcador nuevo ni bump de schema, corrigiendo la previsión del ADR 028 D4 ("marcador anti-insistencia... dentro del bump v23"). El CTA emite el mismo `distribuir:abrir` que ya usaba el recordatorio de Calendario (ADR 021); `tesoreria/index.js` registra el render vía `registrarRender()`, mismo patrón que los paneles de `compromisos`. Reutiliza el componente `.nudge`/`.nudge-info` existente (sin CSS nuevo). 7 tests nuevos (oculto sin ingresos/pendiente/distribuido, visible con "hoy"/"atrasado", CTA emite el evento). E2E smoke 82/82 verde (incluye el flujo "Distribuir abre el asistente").
- 2026-07-05 (IN.6a): saludo dinámico según hora local ("Buenos días/tardes/noches, {nombre}") con `S.perfil.nombre`, sin nombre si está vacío. `updSaludo()` nuevo en `render.js`, invocado desde `renderAll()`; elemento `#saludo-inicio` bajo el título de Inicio. Sin dato nuevo, sin migración (ADR 028 D3). 6 tests nuevos (franjas horarias con reloj falso, sin nombre, `S.perfil` ausente, contenedor ausente).
- 2026-07-05 (IN.7): `renderPanelPrioridades()` excluye `diasRestantes === 0` de los compromisos antes de combinarlos con personales/apartados. Un compromiso que vence hoy vive únicamente en "Pendientes del mes"; personales y apartados que vencen hoy sí siguen apareciendo en "Próximas prioridades" (no tienen panel de vencidos propio). 2 tests migrados de `DIA_HOY` a `DIA_MANANA` (ya no aplica el caso "vence hoy" para ese escenario) + 2 tests nuevos de regresión.

Esta ficha nace del primer análisis exhaustivo de Inicio (2026-07-05), previo a cualquier propuesta de diseño o código para el resto del cluster.

**Observaciones**: el rediseño vive en el [ADR 034](../DECISIONS/034-inicio-v2.md) "Inicio v2" (aceptado el 2026-07-12), que revisa el [ADR 028](../DECISIONS/028-inicio-centro-de-control.md) conservando su principio "un rol por bloque" y toda la plomería de datos (movimientos derivados, accesos data-driven, nudge de distribución). IN.2 (ojo/máscara) evolucionó con el ADR 034 D3/D4 (posición estable + máscara extendida al detalle por cuenta); IN.3 (resumen semanal) evolucionó con el D6 (bloque visual con serie diaria). El avatar de iniciales se deriva de `S.perfil.nombre` existente: NO hizo falta el campo `perfil.avatar` ni el bump v24 que el ADR 028 D3 preveía para IN.6b (el set de avatares ilustrados, si algún día se hace, lo necesitará entonces). **La iniciativa completa (IN.8a a IN.8g) cerró el 2026-07-12**, cada rebanada verificada en la app, con tests y bump de SW propios.
