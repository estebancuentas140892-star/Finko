# Ficha de contexto: Mis cuentas

> Ver reglas de uso y plantilla en [`README.md`](README.md).

---

## Cuentas (dominio `tesoreria`, subsistema cuentas)

- **Objetivo**          : registro de las cuentas del usuario (banco, billetera digital, efectivo, otro) con saldo, cuota de manejo opcional (genera un Compromiso fijo mensual vinculado), GMF (4x1000) opcional, y desde MC.14 datos de transferencia opcionales para consulta rápida. Es la fuente de verdad del saldo total mostrado en Inicio.
- **Estado actual**     : estable, en rediseño visual por rebanadas ([ADR 035](../DECISIONS/035-mis-cuentas-v2.md)). **MC.14** (2026-07-11): cada cuenta admite guardar, opcional, número de cuenta, llave de transferencia + su tipo, y alias, como punto de consulta rápida (Finko no ejecuta transferencias). **MC.15a** (2026-07-11): la tarjeta de la lista ya no repite "Banco de Bogotá · Ahorros" debajo de "Banco de Bogotá Ahorros" cuando el nombre es el autogenerado (el caso normal); en ingresos fijos, la categoría se omite del subtítulo cuando coincide con la descripción. **MC.15c y MC.15d** (2026-07-12): aviso de costos periódicos al crear cuenta + orden categoría→descripción en el ingreso puntual. MC.15b fue absorbida por MC.18b. **MC.18a** (2026-07-12): hero al tope de la sección con el total en cuentas, ojo de privacidad y barra de composición. **MC.18b** (2026-07-12): la lista de cuentas pasa de `.list-item` a `.cuenta-card` con saldo prominente y metadatos como chips (cuota, GMF, datos de transferencia), absorbiendo la legibilidad de logos de la ex MC.15b. **MC.18c** (2026-07-12): el indicador del GMF deja el nudge suelto y pasa a tarjeta insight (`.gmf-insight`) con tinte tesorería.
- **Verificado contra** : commit de MC.18c (2026-07-12).

**Dónde vive**

| Pieza | Archivo | Ancla | Línea |
|---|---|---|---|
| Schema de la cuenta | `modules/core/state.js` | `@typedef Cuenta`, `@typedef DatosTransferencia` (MC.14) | ~19 |
| Consultas (activas, total) | `modules/dominio/tesoreria/logic/cuentas.js` | `cuentasActivas()`, `calcularTotalCuentas()` | ~30, ~40 |
| Composición y resumen para el hero (MC.18a) | `modules/dominio/tesoreria/logic/cuentas.js` | `composicionCuentas()`, `resumenCuentas()` | ~53, ~75 |
| Hero con total en cuentas (MC.18a) | `modules/dominio/tesoreria/views/cuentas.js` | `renderHeroTesoreria()`, contenedor `#tesoreria-hero` | ~40 |
| Ojo de privacidad de la sección (MC.18a) | `modules/dominio/tesoreria/acciones/cuentas.js` | acción `tesoreria-saldo-visibilidad` | ~530 |
| Validación del formulario | `modules/dominio/tesoreria/logic/cuentas.js` | `validarCuenta()` | ~53 |
| Cuota de manejo (opcional, genera Compromiso vinculado) | `modules/dominio/tesoreria/logic/cuentas.js` | `parseCuotaManejo()`, `compromisoDesdeCuotaManejo()`, `compromisoCuotaManejoDeCuenta()` | ~116, ~165 |
| GMF / 4x1000 (lógica) | `modules/dominio/tesoreria/logic/cuentas.js` | `calcularCostoGMF()`, `detectarNudgeGMF()` | |
| GMF / 4x1000, tarjeta insight (MC.18c) | `modules/dominio/tesoreria/views/cuentas.js` | `renderGMFIndicador()`, `_renderGMFInsight()`, contenedor `#tesoreria-gmf` | ~427, ~452 |
| Datos de transferencia (MC.14, opcional) | `modules/dominio/tesoreria/logic/cuentas.js` | `parseDatosTransferencia()` | ~139 |
| Normalización (form crudo → shape de `S.cuentas`) | `modules/dominio/tesoreria/logic/cuentas.js` | `normalizarCuenta()` | ~133 |
| Lista de cuentas + tarjeta individual (MC.18b: `.cuenta-card`) | `modules/dominio/tesoreria/views/cuentas.js` | `renderListaCuentas()`, `_renderCuentaItem()`, `_tipoLabel()` | ~103, ~137, ~123 |
| Chip de datos de transferencia en la tarjeta (MC.14, chip desde MC.18b) | `modules/dominio/tesoreria/views/cuentas.js` | `_labelDatosTransferencia()` | ~205 |
| Formulario del modal (crear/editar) | `modules/dominio/tesoreria/views/cuentas.js` | `renderFormCuenta()` | ~98 |
| Handlers de guardar/editar/eliminar | `modules/dominio/tesoreria/acciones/cuentas.js` | `_guardarCuenta()`, `_editarCuenta()`, `_eliminarCuenta()` | ~72, ~144, ~215 |
| Toggle de campos condicionales según clase de banco | `modules/dominio/tesoreria/acciones/cuentas.js` | `_toggleCamposPorClase()`, `_toggleCuotaFieldset()`, `_toggleTransferenciaFieldset()` (MC.14) | ~287 |
| Wiring del form (submit, toggles, bank-picker) | `modules/dominio/tesoreria/acciones/cuentas.js` | `inyectarFormCuenta()` | ~246 |
| Catálogos: tipos de cuenta, tipos por clase, tipos de llave (MC.14) | `modules/core/constants.js` | `TIPOS_CUENTA`, `TIPOS_POR_CLASE`, `TIPOS_LLAVE` | ~371, ~387, ~394 |

**Recursos**: estilos en `styles/components/*.css` (buscar `.cuenta-card`, `.hero-tesoreria`, `.cuota-fieldset`, `.form-group--checkbox`, `.bank-picker__*`, `.chip`); avatares de banco vía `infra/bancos.js` (`bancoAvatar`, `bancoClase`); catálogo de entidades `BANCOS_CO` en `core/constants.js` (campo `clase`: `banco` | `billetera` | `efectivo` | `otro`, determina qué campos del form se muestran y la etiqueta de tipo de `_tipoLabel()`); iconos `i-agenda`/`i-percent`/`i-key` en `assets/svg/iconos/simbolos/` (sprite regenerado con `scripts/sync-sprite.py`, BR.2).

**Dependencias y relaciones**: `tesoreria/logic/cuentas.js` es puro (sin `S`, sin DOM). `acciones/cuentas.js` usa `infra/crud.js` (`guardar`/`editar`/`eliminar`) para `S.cuentas` y, cuando hay `cuotaManejo`, sincroniza un `Compromiso` fijo vinculado en `S.compromisos` (`_sincronizarCuotaManejo`, único cruce de dominio permitido aquí, vía `infra/crud.js` no vía import directo de `compromisos/logic.js`). Otros dominios leen `S.cuentas` para acreditar/descontar saldo (`compromisos` D.14, `tesoreria` ingresos NAV.A1, `gastos`, abonos) siempre vía `editar('cuentas', ...)` de `infra/crud.js`, nunca importándose entre sí (ADN 10).

**No existe una vista de "detalle de cuenta" separada**: la tarjeta de `_renderCuentaItem()` en la lista (`#lista-tesoreria`) es el único lugar donde se ve una cuenta fuera del formulario de edición. Los datos opcionales (cuota de manejo, GMF, y desde MC.14 los de transferencia) se comunican como chips (MC.18b; antes hints de una línea con emoji) dentro de esa misma tarjeta, no en una pantalla propia.

**Riesgos**:

- **MC.14, ningún campo nuevo requiere bump de `SCHEMA_VERSION`**: `datosTransferencia` es opcional y `undefined`-safe en registros existentes (mismo precedente que `cuotaManejo`/`aplica4x1000`, que tampoco tienen entrada en `_migrate()`). Si en el futuro alguno de estos campos necesitara un valor por defecto distinto de "ausente", ahí sí hace falta migración con backfill.
- **`datosTransferencia` se oculta completo para cuentas tipo Efectivo** (`_toggleCamposPorClase()`, MC.14): el efectivo no tiene número de cuenta ni llave a la que consignar. Si el usuario cambia el banco/billetera de una cuenta que YA tenía datos de transferencia guardados hacia una entidad de clase `efectivo`, el toggle se desmarca y el fieldset se oculta en el form, pero los datos anteriores no se borran del objeto guardado hasta que el usuario guarde el formulario (mismo comportamiento que `cuotaManejo` ya tenía, no es una regresión nueva).
- **`parseDatosTransferencia()` devuelve `null` si el toggle está activo pero todos los campos quedan vacíos tras el `trim()`**: evita guardar un objeto de referencia inútil (`{}` o con solo espacios). El toggle en sí no se persiste, solo su efecto (el objeto o `null`).
- **La regla `llave` requiere `tipoLlave`, pero `numeroCuenta`/`alias` no exigen nada entre sí**: si se agrega un campo nuevo a `DatosTransferencia` en el futuro, decidir explícitamente si necesita esa misma clase de validación cruzada o si es independiente, no asumir.

**Cambios pendientes**: **MC.13** (Distribución v2), **MC.16** (tarjeta de crédito integrada, requiere ADR), **MC.17** (transferencias entre cuentas propias) y **MC.18d a MC.18e** (rediseño visual de la pantalla, ADR 035: fuentes de ingreso agrupadas, distribuir como tarjeta de entrada) siguen pendientes en `docs/BOARD.md`, todos dentro de la iniciativa "Mis Cuentas v2".

**Cambios realizados**:

- 2026-07-12 (MC.18c): `renderGMFIndicador()` (`views/cuentas.js`) pinta `_renderGMFInsight()` en vez de `_renderNudgeGMF()`: el indicador del 4x1000 deja el formato `.nudge nudge-info` (con `border-left` de acento) y pasa a tarjeta propia `.gmf-insight` (teja de 30px con icono `%` fijo, ya no depende del campo `icono` de `detectarNudgeGMF()`), tinte tesorería (7% fondo / 22% borde), en la misma posición de siempre (bajo la lista de cuentas). Cero cambios en `calcularCostoGMF()`/`detectarNudgeGMF()` (`logic/cuentas.js`): mismo cálculo, mismo copy. Contraste WCAG medido en ambos temas (título 13.14:1/15.72:1, detalle 6.68:1/7.96:1 sobre el tinte; glifo 4.59:1/4.19:1 sobre su teja, todos sobre el umbral correspondiente). 2 tests unitarios nuevos + 1 E2E. 2408/2408 unit + 173/173 E2E verdes. SW v358 → v359.

- 2026-07-12 (MC.18b, absorbe MC.15b): `_renderCuentaItem()` (`views/cuentas.js`) pasa de `.list-item` con hints apilados de emoji (📅/💸/🔑) a `.cuenta-card`: teja de banco 44px (`.cuenta-card__icon .bank-avatar`, mejora la legibilidad de logos densos como Davivienda/BBVA/DaviPlata/Nubank sin tocar los SVG oficiales) + nombre + tipo + saldo prominente (`--fk-text-base`/700/tabular) + chips (icono SVG + texto) para cuota de manejo, 4x1000 y datos de transferencia; editar/eliminar como ghost icons `.btn-sm` (32px), eliminar vira a danger en hover (`.cuenta-card__eliminar:hover`). **Nombre/tipo**: generaliza MC.15a con `_tipoLabel(cuenta)` nuevo (usa `bancoClase()` de `infra/bancos.js`): con nombre autogenerado, el título se reduce al banco solo y el tipo (debajo) da la info real ("Ahorros"/"Billetera digital"/"Dinero en efectivo", nunca redundante); con nombre explícito, el título es el nombre y el subtítulo vuelve a ser "banco · tipo" (como antes de MC.18b). **Datos de transferencia**: `_formatDatosTransferencia()` se parte en `_labelDatosTransferencia()` (solo texto) reutilizado por el chip. **Máscara (D5)**: `renderListaCuentas()` lee `S.config.ocultarSaldo` una vez y pasa `oculto` a cada tarjeta; con el ojo activo el saldo por cuenta muestra `SALDO_MASCARA_CUENTA` ('••••') en vez del monto real. **Iconos nuevos**: `assets/svg/iconos/simbolos/key.svg` (`i-key`, trazo + chispa en el ojo de la llave) agregado al sprite vía `scripts/sync-sprite.py`; `i-percent` y `i-agenda` ya existían. CSS `.cuenta-card__*` en `domain.css`, reutiliza `.chip`/`.icon--sm` existentes (cero componente nuevo para los chips). 8 tests unitarios nuevos + 3 reescritos (subtítulo → nombre/tipo) + 1 E2E. 2406/2406 unit + 172/172 E2E verdes. SW v357 → v358.

- 2026-07-12 (MC.18a): hero nuevo al tope de `#sec-tesoreria` (contenedor `#tesoreria-hero` en `index.html`, entre el banner de propósito y la lista). `renderHeroTesoreria()` (`views/cuentas.js`) pinta label "Tu dinero en cuentas" + total (`calcularTotalCuentas()`, existente) + ojo de privacidad + barra de composición + resumen en texto. Lógica pura nueva en `logic/cuentas.js`: `composicionCuentas()` (segmentos por cuenta con saldo positivo, ordenados desc, pct del total) y `resumenCuentas()` ("3 cuentas · 1 billetera · efectivo"). El ojo comparte `S.config.ocultarSaldo` con Inicio (IN.2, un solo control de privacidad): acción nueva `tesoreria-saldo-visibilidad` en `acciones/cuentas.js` (flip + `save()` + `updSaldo()` + re-render de la sección); la máscara usa `SALDO_MASCARA` de `infra/render.js`. Sin cuentas: "Aún no tienes cuentas" + $0, sin ojo ni barra (misma disciplina que `updSaldo()`). CSS `.hero-tesoreria__*` en `domain.css`: degradado de identidad tesorería 16% + borde 30% + sombra en reposo (segundo consumidor del piloto ADR 033), contraste medido (oscuro 11.36/5.78, claro 14.39/7.28); el ojo comparte reglas con `.hero-inicio__ojo` (selectores agrupados, cero duplicación). Ancho/opacidad de segmentos como propiedad JS tras el innerHTML (cero `style=""` en HTML generado). 8 tests unitarios nuevos + 1 E2E. 2403/2403 unit + 171/171 E2E verdes. SW v356 → v357.

- 2026-07-12 (MC.15c + MC.15d): en `tesoreria/views/cuentas.js`, `renderFormCuenta()` agrega el hint `#cuenta-cuota-hint` ("¿Seguro que esta cuenta no cobra cuota de manejo, seguros u otros costos periódicos?") dentro del mismo `.form-group--checkbox` del toggle de cuota, visible solo cuando el toggle está SIN marcar; `_toggleCuotaFieldset()` (`acciones/cuentas.js`) lo oculta al marcar y lo restaura al desmarcar, y el caso "efectivo" de `_toggleCamposPorClase()` ya lo oculta gratis al ocultar el grupo entero. No bloquea el guardado. En `tesoreria/views/ingresos.js`, `renderFormIngresoPuntual()` reordena sus campos: monto → cuenta → categoría → descripción → fecha (antes descripción precedía a categoría), mismo orden que `renderFormIngreso()`; CAT.4 debe saber que este form ya cumple el criterio. 3 tests nuevos en `tests/unit/tesoreria.test.js`. 2394/2394 unit verdes. SW v355 → v356.

- 2026-07-11 (MC.15a): en `tesoreria/views/cuentas.js`, `_renderCuentaItem()` deja de mostrar el subtítulo "banco · tipo" cuando `cuenta.nombre` es el autogenerado por `_autoNombre(banco, tipo)` (el caso normal hoy: el form no ofrece un campo de nombre propio), comparando ambos textos normalizados (trim + lowercase); si el nombre es explícito y difiere (soportado por `normalizarCuenta()` aunque el form actual no lo exponga), el subtítulo se conserva porque sí aporta información nueva. En `tesoreria/views/ingresos.js`, `_renderIngresoItem()` omite la categoría del subtítulo cuando coincide (normalizada) con la descripción del ingreso fijo (ej. descripción "Salario mínimo" + categoría "Salario mínimo" → subtítulo solo "Quincenal"); si difieren, conserva ambas como antes. 5 tests nuevos en `tests/unit/tesoreria.test.js`. 2337/2337 unit verdes. SW v346 → v347.

- 2026-07-11 (MC.14): cada cuenta admite, opcional, número de cuenta, llave de transferencia (con su tipo: Celular/Correo/Documento/Alfanumérico/Otro) y alias, agrupados detrás de un toggle "Guardar los datos que compartes cuando alguien te va a consignar" (mismo patrón de fieldset colapsable que la cuota de manejo). Nuevo catálogo `TIPOS_LLAVE` en `core/constants.js`. `parseDatosTransferencia()` (nuevo, `logic/cuentas.js`) construye el objeto final, `null` si el toggle está apagado o si quedó vacío tras recortar espacios; `validarCuenta()` exige `tipoLlave` cuando hay `llave`. Se muestra como un hint compacto (🔑) en la tarjeta de la lista, igual que los hints existentes de cuota de manejo y GMF. El bloque se oculta para cuentas de clase `efectivo` (sin número de cuenta ni llave que aplique). Sin bump de `SCHEMA_VERSION` (campos opcionales, `undefined`-safe, mismo precedente que `cuotaManejo`/`aplica4x1000`). 18 tests nuevos en `tests/unit/tesoreria.test.js` (validación, parseo, normalización, render del form, render del hint). 2325/2325 unit + 162/162 E2E verdes (1 flake preexistente en `#distribuir-ingreso-panel`, no relacionado, confirmado 3/3 en aislamiento). Primer análisis a fondo de la sección, ficha nueva.
