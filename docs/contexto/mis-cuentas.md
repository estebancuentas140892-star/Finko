# Ficha de contexto: Mis cuentas

> Ver reglas de uso y plantilla en [`README.md`](README.md).

---

## Cuentas (dominio `tesoreria`, subsistema cuentas)

- **Objetivo**          : registro de las cuentas del usuario (banco, billetera digital, efectivo, otro) con saldo, cuota de manejo opcional (genera un Compromiso fijo mensual vinculado), GMF (4x1000) opcional, datos de transferencia opcionales para consulta rápida (MC.14) y, desde MC.17b, transferencias reales entre cuentas propias (desde MC.17c, con rastro visible en el ledger de Movimientos). Es la fuente de verdad del saldo total mostrado en Inicio.
- **Estado actual**     : estable. Rediseño visual completo ([ADR 035](../DECISIONS/035-mis-cuentas-v2.md), MC.18a a MC.18e cerradas). **MC.14** (2026-07-11): cada cuenta admite guardar, opcional, número de cuenta, llave de transferencia + su tipo, y alias, como punto de consulta rápida (dato informativo, no ejecuta nada; distinto de las transferencias reales de MC.17). **MC.15a** (2026-07-11): la tarjeta de la lista ya no repite "Banco de Bogotá · Ahorros" debajo de "Banco de Bogotá Ahorros" cuando el nombre es el autogenerado (el caso normal); en ingresos fijos, la categoría se omite del subtítulo cuando coincide con la descripción. **MC.15c y MC.15d** (2026-07-12): aviso de costos periódicos al crear cuenta + orden categoría→descripción en el ingreso puntual. MC.15b fue absorbida por MC.18b. **MC.18a** (2026-07-12): hero al tope de la sección con el total en cuentas, ojo de privacidad y barra de composición. **MC.18b** (2026-07-12): la lista de cuentas pasa de `.list-item` a `.cuenta-card` con saldo prominente y metadatos como chips (cuota, GMF, datos de transferencia), absorbiendo la legibilidad de logos de la ex MC.15b. **MC.18c** (2026-07-12): el indicador del GMF deja el nudge suelto y pasa a tarjeta insight (`.gmf-insight`) con tinte tesorería. **MC.18d** (2026-07-12): las fuentes de ingreso (fijas + puntuales) se agrupan bajo un solo encabezado "Fuentes de ingreso"; ambas listas ya enmascaran sus montos con el mismo ojo de privacidad. **MC.18e** (2026-07-12, cierra la iniciativa MC.18): "Distribuir mi ingreso" deja de vivir siempre desplegado y pasa a tarjeta compacta (`.distribuir-card`, al final de la sección) con barra 50/30/20 + leyenda; el botón lanza el asistente por pasos (motor MC.7 intacto) como modal nuevo (`#modal-distribuir`). **MC.17b** (2026-07-12): botón "Transferir entre cuentas" (2+ cuentas activas) + modal `#modal-transferencia` que mueve dinero real entre dos cuentas propias. **MC.17c** (2026-07-12): la transferencia aparece en el ledger de Movimientos (`#movimientos` y el panel "Actividad reciente" de Inicio) como una sola fila neutra "Origen → Destino", sin signo ni color, con ícono propio `i-transferencia`. **MC.17d** (2026-07-12): cuando la cuenta de origen no está exenta (`aplica4x1000`), el formulario ofrece descontar el 4x1000 del retiro (checkbox marcado por defecto); sale `monto + costoGMF` del origen, entra `monto` al destino (el patrimonio neto baja exactamente el GMF), se guarda como campo opcional `costoGMF` (no como Gasto) y se traza en el subtítulo del ledger ("incluye $X de 4x1000"). **MC.17e** (2026-07-12, cierra MC.17 completa): teja "Transferir" en la hoja "Registrar" (`modules/ui/registrar.js`), visible solo con 2+ cuentas activas, reutiliza la acción `abrir-transferencia` ya registrada por MC.17b.
- **Verificado contra** : commit de MC.17e (2026-07-12).

**Dónde vive**

| Pieza | Archivo | Ancla | Línea |
|---|---|---|---|
| Schema de la cuenta | `modules/core/state.js` | `@typedef Cuenta`, `@typedef DatosTransferencia` (MC.14) | ~19 |
| Consultas (activas, total) | `modules/dominio/tesoreria/logic/cuentas.js` | `cuentasActivas()`, `calcularTotalCuentas()` | ~30, ~40 |
| Composición y resumen para el hero (MC.18a) | `modules/dominio/tesoreria/logic/cuentas.js` | `composicionCuentas()`, `resumenCuentas()` | ~53, ~75 |
| Hero con total en cuentas (MC.18a) | `modules/dominio/tesoreria/views/cuentas.js` | `renderHeroTesoreria()`, contenedor `#tesoreria-hero` | ~40 |
| Ojo de privacidad de la sección (MC.18a) | `modules/dominio/tesoreria/acciones/cuentas.js` | acción `tesoreria-saldo-visibilidad` | ~530 |
| Validación del formulario | `modules/dominio/tesoreria/logic/cuentas.js` | `validarCuenta()` | ~53 |
| Cuota de manejo (opcional, genera Compromiso vinculado) | `modules/dominio/tesoreria/logic/cuentas.js` | `parseCuotaManejo()`, `compromisoDesdeCuotaManejo()`, `compromisoCuotaManejoDeCuenta()` | ~116, ~165 |
| GMF / 4x1000 (lógica) | `modules/dominio/tesoreria/logic/cuentas.js` | `calcularCostoGMF()`, `detectarNudgeGMF()` | |
| GMF / 4x1000, tarjeta insight (MC.18c) | `modules/dominio/tesoreria/views/cuentas.js` | `renderGMFIndicador()`, `_renderGMFInsight()`, contenedor `#tesoreria-gmf` | ~427, ~452 |
| Datos de transferencia (MC.14, opcional) | `modules/dominio/tesoreria/logic/cuentas.js` | `parseDatosTransferencia()` | ~139 |
| Normalización (form crudo → shape de `S.cuentas`) | `modules/dominio/tesoreria/logic/cuentas.js` | `normalizarCuenta()` | ~133 |
| Lista de cuentas + tarjeta individual (MC.18b: `.cuenta-card`) | `modules/dominio/tesoreria/views/cuentas.js` | `renderListaCuentas()`, `_renderCuentaItem()`, `_tipoLabel()` | ~103, ~137, ~123 |
| Chip de datos de transferencia en la tarjeta (MC.14, chip desde MC.18b) | `modules/dominio/tesoreria/views/cuentas.js` | `_labelDatosTransferencia()` | ~205 |
| Formulario del modal (crear/editar) | `modules/dominio/tesoreria/views/cuentas.js` | `renderFormCuenta()` | ~98 |
| Handlers de guardar/editar/eliminar | `modules/dominio/tesoreria/acciones/cuentas.js` | `_guardarCuenta()`, `_editarCuenta()`, `_eliminarCuenta()` | ~72, ~144, ~215 |
| Toggle de campos condicionales según clase de banco | `modules/dominio/tesoreria/acciones/cuentas.js` | `_toggleCamposPorClase()`, `_toggleCuotaFieldset()`, `_toggleTransferenciaFieldset()` (MC.14) | ~287 |
| Wiring del form (submit, toggles, bank-picker) | `modules/dominio/tesoreria/acciones/cuentas.js` | `inyectarFormCuenta()` | ~246 |
| Catálogos: tipos de cuenta, tipos por clase, tipos de llave (MC.14) | `modules/core/constants.js` | `TIPOS_CUENTA`, `TIPOS_POR_CLASE`, `TIPOS_LLAVE` | ~371, ~387, ~394 |
| Fuentes de ingreso agrupadas (MC.18d) | `modules/dominio/tesoreria/views/ingresos.js` | `renderListaIngresos()`, `renderListaIngresosPuntuales()`, `_renderIngresoItem()`, `_renderIngresoPuntualItem()`, encabezado único en `index.html` | ~24, ~181 |
| Tarjeta de entrada "Distribuir mi ingreso" (MC.18e, `.distribuir-card`) | `modules/dominio/tesoreria/views/distribucion.js` | `_construirDatosDistribucion()`, `renderDistribucionIngreso()`, `_renderTarjetaDistribuir()`, contenedor `#ingresos-distribucion` (al final de la sección) | ~91, ~186, ~515 |
| Asistente por pasos, lanzado como modal (MC.18e) | `modules/dominio/tesoreria/views/distribucion.js` (contenido) + `modules/dominio/tesoreria/acciones/distribucion.js` (apertura/cierre) | `renderAsistenteDistribucion()`, `_renderContenidoAsistente()`, `_renderPanelDistribuir()`; `abrirAsistenteDistribucion()` (único punto de entrada); modal `#modal-distribuir` / `#modal-distribuir-body` en `index.html` | ~215, ~592, ~361; ~143 |
| Lógica pura de transferencias (MC.17a) | `modules/dominio/tesoreria/logic/transferencias.js` | `validarTransferencia()`, `saldoSuficiente()`, `normalizarTransferencia()`, `calcularTransferencia()` (apply atómico puro) | todo el archivo |
| GMF del retiro, lógica pura (MC.17d) | `modules/dominio/tesoreria/logic/transferencias.js` | `costoGMFRetiro()` (monto × 4x1000, redondeado), `origenSujetoAGMF()`; `calcularTransferencia()` descuenta `monto + costoGMF` del origen; `normalizarTransferencia(datos, costoGMF)` guarda `costoGMF` si > 0 | todo el archivo |
| Schema de transferencia (MC.17a/d) | `modules/core/state.js` | `@typedef Transferencia`, colección `S.transferencias`; campo opcional `costoGMF` (MC.17d, sin migración) | |
| Botón de entrada + form + widget de par + sección GMF (MC.17b/d) | `modules/dominio/tesoreria/views/transferencias.js` | `renderBotonTransferir()`, `renderFormTransferencia()`, `renderParTransferencia()`, `renderSeccionGMF()` (checkbox del 4x1000 en `#transferencia-gmf-slot`), contenedor `#tesoreria-transferir` | todo el archivo |
| Acción de transferir + GMF en vivo (MC.17b/d) | `modules/dominio/tesoreria/acciones/transferencias.js` | `_abrirTransferencia()`, `_invertirTransferencia()`, `_guardarTransferencia()`, `_origenActualId()`, `_refrescarSeccionGMF()`, `_actualizarHintGMF()`, modal `#modal-transferencia` en `index.html` | todo el archivo |
| Teja "Transferir" en la hoja Registrar (MC.17e) | `modules/ui/registrar.js` | `cuentasActivasParaTransferir()`, teja en `_construirTejasDinamicas()` (`data-target-action="abrir-transferencia"`) | ~90, ~147 |
| Selector de cuenta reutilizable, extendido para 2 radiogroups (MC.17b) | `modules/infra/cuenta-helper.js` | `renderSelectorCuenta()`, parámetro `name` nuevo (opcional, default `'cuentaId'`) | ~37 |
| Transferencia en el ledger (MC.17c/d) | `modules/dominio/movimientos/logic.js` | `movimientosDesdeTransferencias()`, typedef `Movimiento` (`direccion: 'neutro'`, `tipo: 'transferencia'`, `dominio: 'tesoreria'`, `cuentaOrigenId`/`cuentaDestinoId`, `costoGMF`) | ~113 |
| Descripción "Origen → Destino" en vivo + render sin signo + nota GMF (MC.17c/d) | `modules/dominio/movimientos/view.js` | `_descripcionMovimiento()`, subtítulo "incluye $X de 4x1000" en `_renderMovimientoItem()`, ajustes en `renderActividadReciente()` | ~130 |

**Recursos**: estilos en `styles/components/*.css` (buscar `.cuenta-card`, `.hero-tesoreria`, `.cuota-fieldset`, `.form-group--checkbox`, `.bank-picker__*`, `.chip`, `.distribuir-card`, `.transferir-par`, `.transferir-entrada`); avatares de banco vía `infra/bancos.js` (`bancoAvatar`, `bancoClase`); catálogo de entidades `BANCOS_CO` en `core/constants.js` (campo `clase`: `banco` | `billetera` | `efectivo` | `otro`, determina qué campos del form se muestran y la etiqueta de tipo de `_tipoLabel()`); iconos `i-agenda`/`i-percent`/`i-key`/`i-lightbulb`/`i-chevron-right`/`i-transferencia` (MC.17c, draft propio) en `assets/svg/iconos/simbolos/` (sprite regenerado con `scripts/sync-sprite.py`, BR.2); el botón de invertir del widget de par sigue usando el carácter `⇄` (glifo de acción puntual, no de catálogo de sprite; distinto del ícono del ledger).

**Dependencias y relaciones**: `tesoreria/logic/cuentas.js` es puro (sin `S`, sin DOM). `acciones/cuentas.js` usa `infra/crud.js` (`guardar`/`editar`/`eliminar`) para `S.cuentas` y, cuando hay `cuotaManejo`, sincroniza un `Compromiso` fijo vinculado en `S.compromisos` (`_sincronizarCuotaManejo`, único cruce de dominio permitido aquí, vía `infra/crud.js` no vía import directo de `compromisos/logic.js`). Otros dominios leen `S.cuentas` para acreditar/descontar saldo (`compromisos` D.14, `tesoreria` ingresos NAV.A1, `gastos`, abonos) siempre vía `editar('cuentas', ...)` de `infra/crud.js`, nunca importándose entre sí (ADN 10).

**No existe una vista de "detalle de cuenta" separada**: la tarjeta de `_renderCuentaItem()` en la lista (`#lista-tesoreria`) es el único lugar donde se ve una cuenta fuera del formulario de edición. Los datos opcionales (cuota de manejo, GMF, y desde MC.14 los de transferencia) se comunican como chips (MC.18b; antes hints de una línea con emoji) dentro de esa misma tarjeta, no en una pantalla propia.

**Riesgos**:

- **MC.14, ningún campo nuevo requiere bump de `SCHEMA_VERSION`**: `datosTransferencia` es opcional y `undefined`-safe en registros existentes (mismo precedente que `cuotaManejo`/`aplica4x1000`, que tampoco tienen entrada en `_migrate()`). Si en el futuro alguno de estos campos necesitara un valor por defecto distinto de "ausente", ahí sí hace falta migración con backfill.
- **`datosTransferencia` se oculta completo para cuentas tipo Efectivo** (`_toggleCamposPorClase()`, MC.14): el efectivo no tiene número de cuenta ni llave a la que consignar. Si el usuario cambia el banco/billetera de una cuenta que YA tenía datos de transferencia guardados hacia una entidad de clase `efectivo`, el toggle se desmarca y el fieldset se oculta en el form, pero los datos anteriores no se borran del objeto guardado hasta que el usuario guarde el formulario (mismo comportamiento que `cuotaManejo` ya tenía, no es una regresión nueva).
- **`parseDatosTransferencia()` devuelve `null` si el toggle está activo pero todos los campos quedan vacíos tras el `trim()`**: evita guardar un objeto de referencia inútil (`{}` o con solo espacios). El toggle en sí no se persiste, solo su efecto (el objeto o `null`).
- **La regla `llave` requiere `tipoLlave`, pero `numeroCuenta`/`alias` no exigen nada entre sí**: si se agrega un campo nuevo a `DatosTransferencia` en el futuro, decidir explícitamente si necesita esa misma clase de validación cruzada o si es independiente, no asumir.
- **`#distribuir-ingreso-panel` (MC.18e) ya no existe en el DOM desde el primer render**: solo se inyecta dentro de `#modal-distribuir-body` cuando `abrirAsistenteDistribucion()` lo abre. Cualquier código nuevo que asuma que ese elemento está siempre presente (como sí lo estaba antes de MC.18e, con `hidden`) fallará; usar siempre `abrirAsistenteDistribucion()` como punto de entrada, nunca `document.getElementById('distribuir-ingreso-panel')` directo sin haber abierto el modal primero.

**Cambios pendientes**: **MC.13** (Distribución v2) tiene **análisis y diseño hechos el 2026-07-14** ([ADR 041](../DECISIONS/041-motor-vencimientos-y-distribucion-v2.md), Propuesta; ver el bloque "Distribución v2 y motor de vencimientos" abajo): re-cortada en MC.13a-e, pendiente de aprobación del ADR y de dos decisiones de Esteban. **MC.16** (tarjeta de crédito integrada, requiere ADR) sigue pendiente en `docs/BOARD.md`, dentro de la iniciativa "Mis Cuentas v2". El rediseño visual (ADR 035, MC.18a a MC.18e) y las transferencias entre cuentas (MC.17a a MC.17e) están completos.

---

## Transferencias entre cuentas propias (MC.17, diseño 2026-07-12)

> Bloque de diseño escrito en la fase de análisis (Opus 4.8), **antes de codificar** (regla 2.6). Se implementa por rebanadas MC.17a-e (ver `docs/BOARD.md`), **todas cerradas el 2026-07-12**. **MC.17a**: colección `S.transferencias` + typedef `Transferencia` (`state.js`), bump v25→v26 + migración (`storage.js`), y lógica pura `logic/transferencias.js` (`validarTransferencia`, `saldoSuficiente`, `normalizarTransferencia`, `calcularTransferencia`, exportadas por el barrel `logic.js`). **MC.17b**: botón de entrada + modal + acción, ver "Dónde vive" abajo y el detalle en "Cambios realizados". **MC.17c**: la transferencia aparece en el ledger de Movimientos, ver detalle abajo y en "Cambios realizados". **MC.17d**: GMF del retiro (4x1000) opcional, ver detalle abajo. **MC.17e**: teja "Transferir" en la hoja Registrar, segundo punto de entrada, cierra la iniciativa completa.

- **Objetivo**: mover dinero entre dos cuentas propias en un solo paso, con ambas cuentas actualizadas solas y rastro en el historial. Hoy exige editar dos saldos a mano (propenso a error, sin atomicidad, sin registro).
- **Invariante crítico (lo que no se puede romper)**: una transferencia NO es ingreso ni gasto, es traslado interno. El patrimonio neto (Σ saldos − Σ deudas) no cambia. Por eso NO entra a `S.gastos` (Análisis, Límites de gasto, resumen semanal y monitor de renta no la ven, y así debe ser). En el ledger de Movimientos es un tipo propio con dirección **neutra** (ni `+` verde ni `−` rojo).
- **Modelo de datos**: colección nueva `S.transferencias` (no se deriva de colecciones existentes: no encaja en gastos ni ingresos puntuales). Precedente idéntico: `ingresosPuntuales` (NAV.A1, v22). Shape `Transferencia`: `{ id, cuentaOrigenId, cuentaDestinoId, monto, fecha (YYYY-MM-DD, default hoy editable), nota?, fechaCreacion }`. Bump `SCHEMA_VERSION` v25→v26 con migración idempotente (`if (!Array.isArray(data.transferencias)) data.transferencias = []`) + el campo en `createInitialState()` (segunda red vía `_applyToS`, que restaura cualquier colección ausente del snapshot).
- **Apply atómico**: validar ANTES de tocar saldos (ambas cuentas existen y activas, `cuentaOrigenId !== cuentaDestinoId`, monto > 0, saldo origen suficiente o confirmar sobregiro). Luego débito origen + crédito destino vía `editar('cuentas', ...)` ×2; como todo es síncrono en memoria y `save()` es debounced, ambas mutaciones caen en el mismo flush (no hay ventana de inconsistencia persistida). El helper de apply vive en `logic/transferencias.js` (puro sobre datos) y lo invoca la acción.
- **Automatización por conteo (0/1/2/varias)**: extiende la regla de cuenta única. 0/1 cuenta activa → transferir es imposible (no hay dos endpoints), la entrada no aparece. Exactamente 2 → se muestra "De A a B" con botón para invertir la dirección + solo monto y nota. 3+ → dos selectores independientes de origen y destino. **Implementado en MC.17b**: el selector de destino NO excluye la cuenta elegida en origen (habría exigido sincronizar dos radiogroups en vivo); en su lugar, `validarTransferencia()` bloquea origen == destino con un mensaje claro al confirmar, mismo criterio de "validar al guardar, no complicar el form" que el resto de la app. No es `resolverCuenta()` directo (ese resuelve UNA cuenta): la transferencia tiene su propia lógica de dos endpoints.
- **GMF del retiro (4x1000, MC.17d cerrada)**: decisión ratificada con Esteban el 2026-07-12 (ambas opciones = la recomendación del análisis). Cuando la cuenta origen no está exenta (`aplica4x1000`), el formulario ofrece descontar el 4x1000 con un **checkbox marcado por defecto** (refleja lo que el banco cobrará: el saldo queda exacto; el usuario puede desmarcarlo si tiene el cupo exento del mes). Al aplicar: sale `monto + costoGMF` del origen, entra `monto` al destino → el neto baja EXACTAMENTE el GMF (única parte real-mundo, se lo lleva el banco). Se guarda como campo opcional `costoGMF` en la Transferencia, **NO** como un `Gasto` separado (no ensucia Análisis/Límites con una micro-comisión mecánica). Sin migración (campo opcional `undefined`-safe, precedente `cuotaManejo`/`datosTransferencia`/`aplica4x1000`). La sección del GMF reacciona al origen en vivo: aparece/desaparece al invertir el par o cambiar el selector de 3+ (`_refrescarSeccionGMF`), y el hint recalcula el costo a medida que se escribe el monto (`_actualizarHintGMF`). Salvaguarda dura: `origenSujetoAGMF` en la acción impide descontar el 4x1000 de una cuenta exenta aunque el form dijera lo contrario. Se traza en el subtítulo del ledger (MC.17c) como "incluye $X de 4x1000", sin sumarlo al monto de la fila (que es lo que llegó al destino).
- **Ledger (MC.17c, cerrada)**: `movimientosDesdeTransferencias(transferencias)` en `movimientos/logic.js`. Una transferencia = UNA fila (no dos), dirección `'neutro'`, `tipo: 'transferencia'`, `dominio: 'tesoreria'`, ícono `i-transferencia`. La descripción "Origen → Destino" NO se arma en `logic.js` (que solo recibe `S.transferencias`, sin `cuentas`): se resuelve en vivo en `view.js` (`_descripcionMovimiento()`, usa `_nombreCuenta()` sobre `m.cuentaOrigenId`/`m.cuentaDestinoId`), mismo criterio que el resto del historial (el nombre de cuenta de un gasto tampoco se congela en el momento de la derivación). Los 2 sitios de render (`renderActividadReciente`, `_renderMovimientoItem`) ya no asumen binario `esIngreso ? '+' : '-'`: el signo es `''` cuando `direccion === 'neutro'`. `'transferencias'` se sumó a `_SECCIONES_MOVIMIENTOS` (memo, `view.js`) y a `_SECCIONES_FUENTE` (guard de `state:change`, `movimientos/index.js`; NO en `tesoreria/index.js`, que no muestra el ledger). Símbolo nuevo `i-transferencia` (doble flecha, trazo): draft en `assets/svg/iconos/simbolos/transferencia.svg`, publicado con `scripts/sync-sprite.py` (BR.2); Esteban puede sobrescribirlo (ADR 026).
- **Historial = el ledger**: la vista completa `#movimientos` ES el historial de transferencias. No se crea una lista aparte en Mis cuentas (cero duplicación).
- **Punto de entrada**: dos accesos, ambos cerrados. **MC.17b**: botón "Transferir entre cuentas" en Mis cuentas, bajo la lista de cuentas. **MC.17e**: teja "Transferir" en la hoja "Registrar" (accesible desde cualquier sección), visible solo con 2+ cuentas (patrón 0/1/varias de `registrar.js`), reutiliza la misma acción `abrir-transferencia`.
- **Forward-compat MC.16**: cuando exista la tarjeta de crédito, "pagar la tarjeta" es un abono a deuda, NO una transferencia; las cuentas TC no serán endpoints. MC.17 v1 solo opera cuentas de dinero real. No es dependencia, solo aviso para no modelar la transferencia de forma que estorbe a MC.16.
- **Rebanadas**: MC.17a (schema + lógica pura + apply, solo-unit) → MC.17b (form + acción, patrón 0/1/2/varias) → MC.17c (ledger neutro) → MC.17d (GMF) → MC.17e (teja en Registrar). Las 5 cerradas el 2026-07-12. Detalle en `docs/BOARD.md`.

---

## Distribución v2 y motor de vencimientos (MC.13, diseño 2026-07-14)

> Bloque de diseño escrito en la fase de análisis (Opus 4.8), **antes de codificar** (regla 2.6). Fuente formal: **[ADR 041](../DECISIONS/041-motor-vencimientos-y-distribucion-v2.md)** (estado Propuesta). Nada se implementa sin la aprobación del ADR; dos decisiones son de Esteban (ver abajo).

- **Objetivo**: el asistente "Distribuir mi ingreso" (MC.7) muestra hoy TODAS las obligaciones/aportes del mes y satura. Distribución v2 responde "¿qué toca con ESTE cobro?" usando una ventana derivada de la fecha y frecuencia del ingreso.
- **Hallazgo del análisis (la razón del motor compartido)**: la lógica de "ocurrencias por frecuencia" y "aporte por período" ya existe FRAGMENTADA y DUPLICADA. `_diasParaCompromiso`/`eventosDelMes` (`agenda/logic.js`) resuelven ocurrencias por MES (no por ventana). `construirDesgloseNecesidades` (`tesoreria/logic/distribucion.js`) solo incluye fijos `Mensual` (hueco MC.7g). `ultimoPagoHasta`/`estadoDistribucion` (`ingresos.js`) solo datan Mensual/Quincenal. Y `frecuenciaPrincipalIngresos` + `DIAS_POR_PERIODO` + `calcularAhorroPorPeriodo` están **duplicados** en `metas/logic.js` y `apartados/logic.js` (cada uno se comenta como "duplicado intencional"). Construir un cuarto motor sería el anti-patrón de la regla 2.7.
- **Decisión (ADR 041)**: motor único `modules/infra/vencimientos.js` (puro, sin dominio, ADN #10 intacto, mismo hogar que `estimarSalarioMensual` en `infra/financiero.js`). Dos mitades:
  - **A - vencimientos**: `ocurrenciasEnRango(item, inicioISO, finISO)` (generaliza `_diasParaCompromiso` de mes a ventana; Agenda pasa a consumirla), `ventanaDelCobro(frecuencia, fechaCobroISO)` (ventana del cobro para TODAS las frecuencias).
  - **B - aportes**: `frecuenciaPrincipalIngresos`, `diasPorPeriodo`, `etiquetaPeriodo`, `aportePorPeriodo(faltante, fechaObjetivoISO, frecuencia, hoy)` (una sola copia; Metas y Apartados borran las suyas y re-importan).
  - **Composición**: `obligacionesYAportesDelCobro({cobro, compromisos, gastos, metas, apartados, fondo, hoy})` → `{ ventana, vencidas, enVentana, aportes }`. Solo lo del cobro, con vencidas separadas de por-vencer (copy de los puntos 9-10). Absorbe MC.7g (todas las frecuencias).
- **Consumidores** (un motor, no cuatro): asistente v2, checklist de Necesidades (ex MC.7g), Metas (`calcularAhorroPorPeriodo` → `aportePorPeriodo`, MT.6), Apartados (prellenar Aportar, AP.5), fondo en la distribución (AH.5), pagos automáticos (PA.1, `vencidas` en catch-up).
- **Schema (D5)**: campo opcional `cuentaId` en `@typedef Ingreso` (hoy NO lo tiene; `IngresoPuntual` sí). Bump v26→v27, migración aditiva idempotente (ausente → comportamiento actual; sin backfill: adivinar la cuenta sería inventar el dato). Registrar la cuenta NO implica crédito automático (eso es el conflicto (b), materia de PA.1).
- **Decisiones pendientes de Esteban (regla 2.7)**: (a) quitar la oferta automática de distribución tras un ingreso esporádico revierte NAV.A2b s2 del ADR 024 (`_ofrecerDistribucion` en `tesoreria/acciones/ingresos.js`); recomendación del análisis: quitar la oferta automática, conservar el asistente accesible manualmente. (b) crédito automático del ingreso fijo = PA.1, fuera de este ADR.
- **Rebanadas**: **MC.13a cerrada 2026-07-14** (motor mitad A + Agenda consume, ver abajo) → MC.13b (motor mitad B + Metas/Apartados borran copias) → MC.13c (`obligacionesYAportesDelCobro` + checklist) → MC.13d (schema `cuentaId` + form, independiente) → MC.13e+ (asistente v2 UI, puntos 9-20; depende de la decisión (a)). Detalle en `docs/BOARD.md` y el ADR 041.

**Dónde vive (MC.13a)**

| Pieza | Archivo | Ancla |
|---|---|---|
| Motor mitad A (ocurrencias + ventana) | `modules/infra/vencimientos.js` | `ocurrenciasEnMes`, `ocurrenciasEnRango`, `ventanaDelCobro` |
| Agenda consume el motor | `modules/dominio/agenda/logic.js` | `eventosDelMes`, `eventosIngresosDelMes` (import de `ocurrenciasEnMes`) |

---

**Cambios realizados**:

- 2026-07-14 (MC.13a, primera rebanada de Distribución v2): `modules/infra/vencimientos.js` nuevo (puro, sin dominio, ADN #10, hogar de infra igual que `estimarSalarioMensual`): `ocurrenciasEnMes` extrae la regla de frecuencias que estaba en `_diasParaCompromiso` de Agenda (con la validación de `diaPago` incorporada: devuelve `[]` si es inválido), `ocurrenciasEnRango` la generaliza a una ventana arbitraria que cruza meses, `ventanaDelCobro` da la ventana del cobro para TODAS las frecuencias (Mensual usa el mes calendario con clamp; a diferencia de `ultimoPagoHasta`, sólo Mensual/Quincenal). `agenda/logic.js`: `eventosDelMes`/`eventosIngresosDelMes` pasan a consumir `ocurrenciasEnMes` y se borran los helpers duplicados (`_diasParaCompromiso`, `_caeEnCiclo`, `_diasDelMes`, `_parseFechaISO`); comportamiento idéntico. 33 tests unitarios nuevos (`vencimientos.test.js`); los 139 de `agenda.test.js` son la red de regresión. 2728/2728 unit + 205/205 E2E + lint verdes. SW v395→v396.

- 2026-07-12 (MC.17e, cierra MC.17 completa): quinta y última rebanada de transferencias, segundo punto de entrada. `modules/ui/registrar.js`: `cuentasActivasParaTransferir(cuentas)` (nueva, pura, cuenta cuentas con `activa !== false` sin importar el dominio `tesoreria`, mismo patrón local que `_TIPOS_DEUDA` en el mismo archivo, regla ADN #10) + teja "Transferir" en `_construirTejasDinamicas()` (visible solo con 2+ cuentas activas, mismo patrón 0/1/varias que Abono a deuda y Aporte a ahorro), `data-action="registrar-abrir" data-target-action="abrir-transferencia"` reutiliza la acción ya registrada por MC.17b: cero lógica nueva de apertura de modal. 2 tests unitarios nuevos (`registrar.test.js`). Verificación funcional vía DOM en el Browser pane (el screenshot no respondió esta sesión): con 2 cuentas activas la teja aparece y su clic abre `#modal-transferencia`; con 1 cuenta, no se inyecta. 2479/2479 unit verdes. SW v365 → v366.

- 2026-07-12 (MC.17d): cuarta rebanada de transferencias, el GMF (4x1000) del retiro. Decisión ratificada con Esteban (campo `costoGMF` en la Transferencia, no un Gasto; checkbox marcado por defecto). `logic/transferencias.js`: `costoGMFRetiro(monto)` (pura, monto × 0.004 redondeado, 0 si monto no positivo), `origenSujetoAGMF(cuentas, id)` (salvaguarda dura: solo `aplica4x1000 === true`); `normalizarTransferencia(datos, costoGMF)` agrega `costoGMF` si > 0; `calcularTransferencia` descuenta `monto + costoGMF` del origen (Σ deltas = −costoGMF: el neto baja exactamente el gravamen). `views/transferencias.js`: `renderSeccionGMF(origen)` pinta el checkbox del 4x1000 (marcado) solo cuando el origen no está exento, dentro de `#transferencia-gmf-slot` (contenedor estable). `acciones/transferencias.js`: `_refrescarSeccionGMF()` re-renderiza el slot al cambiar el origen (invertir o selector 3+), `_actualizarHintGMF()` recalcula el costo en vivo con el monto; `_guardarTransferencia()` calcula `costoGMF` (solo si origen no exento + checkbox marcado), usa `monto + costoGMF` para el chequeo de sobregiro y el mensaje de confirmación, y lo anuncia. `state.js`: campo opcional `costoGMF` en `@typedef Transferencia` (sin migración). Ledger (extiende MC.17c): `movimientosDesdeTransferencias` propaga `costoGMF`; `_renderMovimientoItem` lo traza en el subtítulo ("incluye $X de 4x1000") sin sumarlo al monto de la fila. La transferencia sigue sin tocar `S.gastos` (el GMF no es un Gasto): Análisis/Límites/monitor de renta no lo ven. 21 tests unitarios nuevos (`tesoreria.test.js` + `movimientos.test.js`) + 1 E2E nuevo (flujo completo + rastro en el ledger, Chromium real). 2477/2477 unit + 179/179 E2E verdes. SW v364 → v365.

- 2026-07-12 (MC.17c): tercera rebanada de transferencias, el ledger de Movimientos. `movimientosDesdeTransferencias(transferencias)` (nuevo, `movimientos/logic.js`) normaliza `S.transferencias` a `Movimiento` con `direccion: 'neutro'`, `tipo: 'transferencia'`, `dominio: 'tesoreria'`, ícono `i-transferencia`, y guarda `cuentaOrigenId`/`cuentaDestinoId` en vez de una descripción congelada (la fuente pura no recibe `cuentas`, solo `transferencias`). `view.js` arma "Origen → Destino" en vivo (`_descripcionMovimiento()`, reutiliza `_nombreCuenta()`); `renderActividadReciente()` y `_renderMovimientoItem()` ganan un tercer caso de signo (`direccion === 'neutro'` → sin `+`/`-`, misma clase de color que egreso, ya neutra por defecto en `atoms.css`/`domain.css`: sin CSS nuevo). `_TIPO_LABEL` suma `transferencia: 'Transferencia'` para el subtítulo de la vista completa. `'transferencias'` se agregó a `_SECCIONES_MOVIMIENTOS` (memo, `view.js`) y a `_SECCIONES_FUENTE` (guard de `state:change`, `movimientos/index.js`), así que guardar una transferencia (MC.17b) re-pinta el panel de Inicio y `#movimientos` solos. Símbolo de sprite nuevo `i-transferencia` (doble flecha, `assets/svg/iconos/simbolos/transferencia.svg`, draft propio publicado con `scripts/sync-sprite.py`); Esteban puede sobrescribirlo (ADR 026). La transferencia NO altera Análisis/Límites/monitor de renta (no toca `S.gastos`, invariante de MC.17 intacto). 12 tests unitarios nuevos (`movimientos.test.js`). 2456/2456 unit verdes. SW v363 → v364.

- 2026-07-12 (MC.17b): segunda rebanada de transferencias, UI sobre la lógica pura de MC.17a. `views/transferencias.js` (nuevo): `renderBotonTransferir()` pinta el botón "Transferir entre cuentas" en `#tesoreria-transferir` solo con 2+ cuentas activas (patrón 0/1/2/varias); `renderFormTransferencia()` arma el modal según el conteo: con exactamente 2, `renderParTransferencia(origen, destino)` (widget fijo, origen por defecto = mayor saldo, botón `⇄` que invierte reemplazando solo `#transferencia-par-wrap`); con 3+, dos `renderSelectorCuenta()` independientes (`name: 'cuentaOrigenId'`/`'cuentaDestinoId'`, sin excluir la cuenta ya elegida en el otro selector: `validarTransferencia()` bloquea origen == destino al confirmar, decisión documentada arriba). `renderSelectorCuenta()` (`infra/cuenta-helper.js`) gana el parámetro opcional `name` (default `'cuentaId'`, retrocompatible con sus 5 callers existentes). `acciones/transferencias.js` (nuevo): `_guardarTransferencia()` valida, confirma sobregiro con `confirmar()` si `!saldoSuficiente()` (mismo patrón "Registrar igual" de `compromisos/index.js`), aplica `calcularTransferencia()` vía `editar('cuentas', ...)` ×2, persiste en `S.transferencias` con `guardar()` y cierra el modal. Modal `#modal-transferencia` nuevo en `index.html`. CSS `.transferir-entrada`/`.transferir-par*` en `domain.css`. 13 tests unitarios nuevos (`tesoreria.test.js`) + 4 E2E nuevos (`smoke.test.js`: gating 0/1/2/varias, transferencia con 2 cuentas actualiza ambos saldos, invertir, transferencia con 3+ cuentas). `service-worker.js` v362→v363 + los 2 módulos nuevos en `CORE_ASSETS`. 2449/2449 unit + 178/178 E2E verdes. Nota de proceso: el Browser pane interactivo no estuvo disponible esta sesión (clasificador de seguridad caído); la verificación se apoyó en los E2E de Playwright sobre Chromium real, sin revisión visual manual del widget nuevo.

- 2026-07-12 (MC.17a): primera rebanada de transferencias entre cuentas, solo fundación de datos + lógica pura (verificación solo-unit). `state.js`: typedef `Transferencia` + colección `S.transferencias` en `createInitialState()`. `storage.js`: `SCHEMA_VERSION` 25→26 + migración idempotente y aditiva v25→v26 (`transferencias: []`, no toca el resto). `logic/transferencias.js` (nuevo, puro): `validarTransferencia(datos, cuentas)` (origen/destino existen y activos, distintos, monto > 0, fecha ISO; el saldo insuficiente NO es error, lo confirma la UI), `saldoSuficiente(cuentas, origenId, monto)`, `normalizarTransferencia(datos)` (omite nota vacía), `calcularTransferencia(transferencia, cuentas)` (apply atómico PURO: 2 actualizaciones de saldo + deltas, o `null` como guard estructural). Invariante verificado: Σ deltas = 0 (patrimonio neto invariante). `logic.js` barrel exporta las 4. `service-worker.js` v361→v362 + `logic/transferencias.js` en `CORE_ASSETS`. GMF diferido a MC.17d (la función es monto-based). 3 tests de migración (`storage.test.js`) + 23 de las funciones puras (`tesoreria.test.js`) + 1 ajustado (`flujos.test.js`, que usaba `transferencias` como campo desconocido). 2436/2436 unit + 174/174 E2E verdes.

- 2026-07-12 (MC.18e, cierra la iniciativa MC.18): `renderDistribucionIngreso()` (`views/distribucion.js`) deja de pintar el bloque siempre desplegado (chips de preset, desglose, editor personalizado, asistente inline) y pasa a tarjeta compacta `.distribuir-card`: "¿Cómo distribuir $X?" con barra segmentada 50/30/20 + leyenda (Necesidades/Estilo de vida/Ahorro, cada una con % y monto) + alertas + CTA cruzado a Límites de gasto (MC.5e, siempre visibles) + botón que lanza el asistente. Los 3 colores reutilizan tokens de dominio existentes sin hex nuevos: Necesidades = `--fk-dom-tesoreria`, Estilo de vida = `--fk-dom-presupuesto`, Ahorro = `--fk-dom-ahorro` (coinciden con la paleta del handoff). **El asistente por pasos (motor MC.7, MC.4a/b/d/e) no cambió de lógica, solo de casa**: `_renderPanelDistribuir()` sigue generando el mismo `<fieldset id="distribuir-ingreso-panel">`, pero ahora se inyecta en `#modal-distribuir-body` (`renderAsistenteDistribucion()`, nueva función exportada) cada vez que se abre el modal `#modal-distribuir` (nuevo en `index.html`), en vez de vivir siempre en el DOM con `hidden` + su propio botón toggle. Los chips de preset/clásicos/personalizada y la razón del cálculo se mudaron junto al asistente, dentro del modal (`_renderContenidoAsistente()`, ex `_renderDistribucion()`). `abrirAsistenteDistribucion()` (`acciones/distribucion.js`) sigue siendo el único punto de entrada para todos los callers (botón de la tarjeta, recordatorio de Calendario ADR 021, oferta tras ingreso puntual NAV.A2b s2): ninguno cambió su firma. Confirmar la distribución cierra el modal. La tarjeta se reubicó al final de `#sec-tesoreria` (orden ADR 035 completo). `data-action="toggle-distribuir-ingreso"` se conserva por nombre (abre el modal, ya no destogglea un panel) para no repintar los ~30 call sites de E2E que ya lo usan. Sin tests unitarios nuevos (sin precedente en esta capa); 1 E2E ajustado (conteo de `.section__sub-header` escopado por texto, la tarjeta agrega un segundo sub-header legítimo). 2410/2410 unit + 174/174 E2E verdes. SW v360 → v361.

- 2026-07-12 (MC.18d): los dos `.section__sub-header` independientes de `#sec-tesoreria` ("Mis ingresos fijos" con "+ Ingreso fijo", "Otros ingresos" con "+ Ingreso") se fusionan en uno solo, "Fuentes de ingreso", con las dos acciones ("+ Fijo" / "+ Puntual") lado a lado en `.section__sub-actions` (nuevo, `styles/layout.css`, envoltorio genérico reutilizable). **Resolución del CTA único del mockup:** se mantuvieron los dos botones/formularios existentes (uno crea un ingreso recurrente, el otro uno de una sola vez, con campos distintos) en vez de introducir un selector nuevo (menú/popover); documentado como decisión de esta rebanada, no una pieza de interacción nueva. Ambas listas (`#lista-ingresos`, `#lista-ingresos-puntuales`) quedan una tras otra sin sub-header propio entre ellas. **Estilo de tarjeta:** `#lista-ingresos .list-item, #lista-ingresos-puntuales .list-item` (selector scoped por contenedor en `domain.css`, sin tocar la `.list-item` base de las demás secciones) ganan `border-radius: var(--fk-radius-lg)` + `box-shadow: var(--fk-shadow-sm)`, mismo lenguaje que `.cuenta-card` (tercer y último consumidor del piloto ADR 033 en esta pantalla). **Máscara (D5):** `renderListaIngresos()`/`renderListaIngresosPuntuales()` leen `S.config.ocultarSaldo` una vez y pasan `oculto` a cada item; enmascarado usa `SALDO_MASCARA_CUENTA` ('••••'), el puntual conserva el prefijo `+` (`+••••`). 2 tests unitarios nuevos + 1 E2E. 2410/2410 unit + 174/174 E2E verdes. SW v359 → v360.

- 2026-07-12 (MC.18c): `renderGMFIndicador()` (`views/cuentas.js`) pinta `_renderGMFInsight()` en vez de `_renderNudgeGMF()`: el indicador del 4x1000 deja el formato `.nudge nudge-info` (con `border-left` de acento) y pasa a tarjeta propia `.gmf-insight` (teja de 30px con icono `%` fijo, ya no depende del campo `icono` de `detectarNudgeGMF()`), tinte tesorería (7% fondo / 22% borde), en la misma posición de siempre (bajo la lista de cuentas). Cero cambios en `calcularCostoGMF()`/`detectarNudgeGMF()` (`logic/cuentas.js`): mismo cálculo, mismo copy. Contraste WCAG medido en ambos temas (título 13.14:1/15.72:1, detalle 6.68:1/7.96:1 sobre el tinte; glifo 4.59:1/4.19:1 sobre su teja, todos sobre el umbral correspondiente). 2 tests unitarios nuevos + 1 E2E. 2408/2408 unit + 173/173 E2E verdes. SW v358 → v359.

- 2026-07-12 (MC.18b, absorbe MC.15b): `_renderCuentaItem()` (`views/cuentas.js`) pasa de `.list-item` con hints apilados de emoji (📅/💸/🔑) a `.cuenta-card`: teja de banco 44px (`.cuenta-card__icon .bank-avatar`, mejora la legibilidad de logos densos como Davivienda/BBVA/DaviPlata/Nubank sin tocar los SVG oficiales) + nombre + tipo + saldo prominente (`--fk-text-base`/700/tabular) + chips (icono SVG + texto) para cuota de manejo, 4x1000 y datos de transferencia; editar/eliminar como ghost icons `.btn-sm` (32px), eliminar vira a danger en hover (`.cuenta-card__eliminar:hover`). **Nombre/tipo**: generaliza MC.15a con `_tipoLabel(cuenta)` nuevo (usa `bancoClase()` de `infra/bancos.js`): con nombre autogenerado, el título se reduce al banco solo y el tipo (debajo) da la info real ("Ahorros"/"Billetera digital"/"Dinero en efectivo", nunca redundante); con nombre explícito, el título es el nombre y el subtítulo vuelve a ser "banco · tipo" (como antes de MC.18b). **Datos de transferencia**: `_formatDatosTransferencia()` se parte en `_labelDatosTransferencia()` (solo texto) reutilizado por el chip. **Máscara (D5)**: `renderListaCuentas()` lee `S.config.ocultarSaldo` una vez y pasa `oculto` a cada tarjeta; con el ojo activo el saldo por cuenta muestra `SALDO_MASCARA_CUENTA` ('••••') en vez del monto real. **Iconos nuevos**: `assets/svg/iconos/simbolos/key.svg` (`i-key`, trazo + chispa en el ojo de la llave) agregado al sprite vía `scripts/sync-sprite.py`; `i-percent` y `i-agenda` ya existían. CSS `.cuenta-card__*` en `domain.css`, reutiliza `.chip`/`.icon--sm` existentes (cero componente nuevo para los chips). 8 tests unitarios nuevos + 3 reescritos (subtítulo → nombre/tipo) + 1 E2E. 2406/2406 unit + 172/172 E2E verdes. SW v357 → v358.

- 2026-07-12 (MC.18a): hero nuevo al tope de `#sec-tesoreria` (contenedor `#tesoreria-hero` en `index.html`, entre el banner de propósito y la lista). `renderHeroTesoreria()` (`views/cuentas.js`) pinta label "Tu dinero en cuentas" + total (`calcularTotalCuentas()`, existente) + ojo de privacidad + barra de composición + resumen en texto. Lógica pura nueva en `logic/cuentas.js`: `composicionCuentas()` (segmentos por cuenta con saldo positivo, ordenados desc, pct del total) y `resumenCuentas()` ("3 cuentas · 1 billetera · efectivo"). El ojo comparte `S.config.ocultarSaldo` con Inicio (IN.2, un solo control de privacidad): acción nueva `tesoreria-saldo-visibilidad` en `acciones/cuentas.js` (flip + `save()` + `updSaldo()` + re-render de la sección); la máscara usa `SALDO_MASCARA` de `infra/render.js`. Sin cuentas: "Aún no tienes cuentas" + $0, sin ojo ni barra (misma disciplina que `updSaldo()`). CSS `.hero-tesoreria__*` en `domain.css`: degradado de identidad tesorería 16% + borde 30% + sombra en reposo (segundo consumidor del piloto ADR 033), contraste medido (oscuro 11.36/5.78, claro 14.39/7.28); el ojo comparte reglas con `.hero-inicio__ojo` (selectores agrupados, cero duplicación). Ancho/opacidad de segmentos como propiedad JS tras el innerHTML (cero `style=""` en HTML generado). 8 tests unitarios nuevos + 1 E2E. 2403/2403 unit + 171/171 E2E verdes. SW v356 → v357.

- 2026-07-12 (MC.15c + MC.15d): en `tesoreria/views/cuentas.js`, `renderFormCuenta()` agrega el hint `#cuenta-cuota-hint` ("¿Seguro que esta cuenta no cobra cuota de manejo, seguros u otros costos periódicos?") dentro del mismo `.form-group--checkbox` del toggle de cuota, visible solo cuando el toggle está SIN marcar; `_toggleCuotaFieldset()` (`acciones/cuentas.js`) lo oculta al marcar y lo restaura al desmarcar, y el caso "efectivo" de `_toggleCamposPorClase()` ya lo oculta gratis al ocultar el grupo entero. No bloquea el guardado. En `tesoreria/views/ingresos.js`, `renderFormIngresoPuntual()` reordena sus campos: monto → cuenta → categoría → descripción → fecha (antes descripción precedía a categoría), mismo orden que `renderFormIngreso()`; CAT.4 debe saber que este form ya cumple el criterio. 3 tests nuevos en `tests/unit/tesoreria.test.js`. 2394/2394 unit verdes. SW v355 → v356.

- 2026-07-11 (MC.15a): en `tesoreria/views/cuentas.js`, `_renderCuentaItem()` deja de mostrar el subtítulo "banco · tipo" cuando `cuenta.nombre` es el autogenerado por `_autoNombre(banco, tipo)` (el caso normal hoy: el form no ofrece un campo de nombre propio), comparando ambos textos normalizados (trim + lowercase); si el nombre es explícito y difiere (soportado por `normalizarCuenta()` aunque el form actual no lo exponga), el subtítulo se conserva porque sí aporta información nueva. En `tesoreria/views/ingresos.js`, `_renderIngresoItem()` omite la categoría del subtítulo cuando coincide (normalizada) con la descripción del ingreso fijo (ej. descripción "Salario mínimo" + categoría "Salario mínimo" → subtítulo solo "Quincenal"); si difieren, conserva ambas como antes. 5 tests nuevos en `tests/unit/tesoreria.test.js`. 2337/2337 unit verdes. SW v346 → v347.

- 2026-07-11 (MC.14): cada cuenta admite, opcional, número de cuenta, llave de transferencia (con su tipo: Celular/Correo/Documento/Alfanumérico/Otro) y alias, agrupados detrás de un toggle "Guardar los datos que compartes cuando alguien te va a consignar" (mismo patrón de fieldset colapsable que la cuota de manejo). Nuevo catálogo `TIPOS_LLAVE` en `core/constants.js`. `parseDatosTransferencia()` (nuevo, `logic/cuentas.js`) construye el objeto final, `null` si el toggle está apagado o si quedó vacío tras recortar espacios; `validarCuenta()` exige `tipoLlave` cuando hay `llave`. Se muestra como un hint compacto (🔑) en la tarjeta de la lista, igual que los hints existentes de cuota de manejo y GMF. El bloque se oculta para cuentas de clase `efectivo` (sin número de cuenta ni llave que aplique). Sin bump de `SCHEMA_VERSION` (campos opcionales, `undefined`-safe, mismo precedente que `cuotaManejo`/`aplica4x1000`). 18 tests nuevos en `tests/unit/tesoreria.test.js` (validación, parseo, normalización, render del form, render del hint). 2325/2325 unit + 162/162 E2E verdes (1 flake preexistente en `#distribuir-ingreso-panel`, no relacionado, confirmado 3/3 en aislamiento). Primer análisis a fondo de la sección, ficha nueva.
