# Ficha de contexto: Configuración (Ajustes)

> Revisado: 2026-08-13 (CFG.6).

> Ver reglas de uso y plantilla en [`README.md`](README.md).

---

## Panel de Ajustes (dominio `config`)

- **Objetivo**          : centro de configuración de Finko. Desde 2026-07-25 el panel se emite en **cinco grupos rotulados** (`La app` · `Tu cuenta` · `Tus datos` · `Impuestos` · `Información`), no como pila plana: apariencia y recordatorios abren la sección, perfil fiscal (K.2) y datos de renta (K.4) viven plegados en un `<details>`, y el Centro Legal y "Acerca de" cierran. Es un dominio de UI: lee `S`, muta directamente (sin EventBus para sus propios formularios) y **ya no re-renderiza el panel entero tras guardar** un formulario: confirma con un chip visible y deja el DOM en su sitio.
- **Estado actual**     : estable. **CFG.1a** (2026-07-06) reemplazó el campo SMMLV muerto del encabezado por la situación laboral (schema v25). **CFG.2b** (2026-08-03) cerró el framing legal del ADR 050 y le dio consumidor a la situación laboral. **CFG.2a** (2026-08-13) hizo que "Ingresos brutos" se derive de `S.ingresos` + `S.ingresosPuntuales`. **CFG.2c** (2026-08-13) ejecutó D1 del ADR 050 y **cierra la iniciativa fusionada CFG.1+CFG.2**: "Perfil fiscal y datos de renta" deja de montarse en el panel (ni plegado) y pasa a un asistente (`#modal-fiscal`) que se abre tras el botón "Completar perfil fiscal" del grupo "Impuestos". **CFG.5a** (2026-08-12) sumó el candado de acceso local con PIN (schema v36, [ADR 063](../DECISIONS/063-candado-de-acceso-local.md)); de esa iniciativa quedan CFG.5b (re-autenticación de acciones críticas) y CFG.5c (viabilidad de biometría). **La iniciativa CFG.3 (avisos anticipatorios) cierra completa el 2026-08-13** ([ADR 066](../DECISIONS/066-motor-unico-de-avisos.md)): CFG.3a (motor + notificación), CFG.3b (panel en Inicio, ficha en `contexto/inicio.md`) y **CFG.3c** (preferencias por sección + sello de "ya avisó hoy", schema v40). El panel de Ajustes sí cambia en CFG.3c: la sección "Recordatorios" gana un `<fieldset>` con cinco interruptores (uno por `SECCIONES_AVISO` de `infra/avisos.js`), visible solo con los recordatorios activos. Detalle abajo, en "Recordatorios (avisos, CFG.3)". **CFG.6 cierra completa el 2026-08-13**: el inventario de configs faltantes (punto 1, su único frente abierto) no encontró hallazgos nuevos. Otras tarjetas del BOARD sin iniciar: CFG.4 (respaldo, roza ADN), LEG.1 rebanada de UI (Centro Legal que muestre los textos de `docs/legal/`, ya redactados el 2026-07-09).
- **Verificado contra** : CFG.6 (2026-08-13, inventario final del panel; sin cambios de código).

**Dónde vive**

| Pieza | Archivo | Ancla | Línea |
|---|---|---|---|
| Orquestador del render (cinco grupos rotulados) | `modules/dominio/config/view.js` | `renderPanelConfig()`, `_grupo()` | ~58, ~78 |
| Separador de miles de los campos de renta | `modules/dominio/config/view.js` | `miles()`, `desdeMiles()` | ~24, ~40 |
| Botón de guardar + chip de confirmación | `modules/dominio/config/view.js` | `_botonGuardar()` | ~96 |
| Perfil (nombre + situación laboral) | `modules/dominio/config/view.js` | `_renderPerfil()` | ~173 |
| Botón de entrada al asistente fiscal (CFG.2c) | `modules/dominio/config/view.js` | `_renderImpuestos()` | ~280 |
| Contenido del asistente fiscal (perfil + datos de renta), inyectado al abrir | `modules/dominio/config/view.js` | `renderModalFiscal()`, `_renderPerfilFiscal()`, `_renderDatosRenta()` | ~296 |
| Modal del asistente fiscal | `index.html` | `#modal-fiscal`, `#modal-fiscal-body` | ~1508 |
| Abrir el asistente + cablear sus dos formularios | `modules/dominio/config/index.js` | `_abrirPerfilFiscal()`, `_wireFormularioFiscal()` | ~230 |
| Aviso de almacenamiento (ADR 030) | `modules/dominio/config/view.js` | `_renderAvisoAlmacenamiento()` | ~383 |
| Recordatorios: interruptor por sección de aviso (CFG.3c) | `modules/dominio/config/view.js` | `_renderPreferenciasAvisos()` | ~344 |
| Recordatorios: handler del interruptor por sección | `modules/dominio/config/index.js` | `_toggleAvisoSeccion()` | ~130 |
| Motor de avisos: secciones, etiquetas y filtro por preferencia | `modules/infra/avisos.js` | `SECCIONES_AVISO`, `LABEL_SECCION_AVISO`, `filtrarPorPreferencia()` | |
| Migración v39 → v40 (`config.avisosPorSeccion`, `.ultimoAvisoISO`) | `modules/core/storage.js` | bloque `< 40` en `_migrate` | ~592 |
| Gestión de datos (respaldo/CSV/borrar todo) | `modules/dominio/config/view.js` | `_renderDatos()` | ~426 |
| Confirmación visible tras guardar | `modules/dominio/config/index.js` | `_confirmarGuardado()` | ~40 |
| Init + handlers de formularios y acciones | `modules/dominio/config/index.js` | `initConfig()`, `_inyectarPanel()` | ~285, ~215 |
| Situaciones laborales (catálogo, CFG.1) | `modules/core/constants.js` | `SITUACIONES_LABORALES` | ~547 |
| Campo del estado | `modules/core/state.js` | `perfil.situacionLaboral` | ~283 |
| Migración v24 → v25 | `modules/core/storage.js` | bloque `< 25` en `_migrate` | ~407 |
| Candado de acceso: bloque de Ajustes (CFG.5a) | `modules/dominio/config/view.js` | `_renderBloqueo()` | ~239 |
| Candado de acceso: lógica sin DOM (hash, freno) | `modules/dominio/config/bloqueo.js` | `crearBloqueo()`, `verificarPin()`, `registrarFallo()` | ~120, ~140, ~175 |
| Candado de acceso: gate de arranque | `modules/ui/bloqueo-acceso.js` | `initBloqueoAcceso()`, `faltaDesbloquear()` | ~35, ~30 |
| Migración v35 → v36 (`config.bloqueo`) | `modules/core/storage.js` | bloque `< 36` en `_migrate` | ~554 |
| Centro Legal: lista + sección (LEG.1) | `modules/dominio/config/view.js` | `_renderLegal()` | ~367 |
| Centro Legal: catálogo (con `destacado`) + marca de borrador | `modules/dominio/config/legal.js` | `DOCUMENTOS_LEGALES`, `VERSION_LEGAL`, `cargarDocumentoLegal()` | ~29, ~20, ~60 |
| Centro Legal: abrir/cambiar documento | `modules/dominio/config/index.js` | `_mostrarDocumentoLegal()`, `_wireLegalLinks()` | ~135, ~163 |
| Conversor Markdown → HTML (sin dependencias) | `modules/infra/markdown.js` | `mdToHtml()` | ~68 |
| Modal genérico del visor | `index.html` | `#modal-legal` | ~1079 |

**Recursos**: estilos en `styles/components/config.css` (`.config-grupo`, `.config-grupo__label`, `.config-desplegable`, `.config-bloque`, `.config-section`, `.config-info`, `.config-form`, `.config-form__guardar`, `.config-toggle`, `.config-danger`, `.config-actions`, `.config-actions--ambito`, `.legal-lista`, `.legal-doc`). Constantes: `SITUACIONES_LABORALES`, `SMMLV`/`legalVigente`/`APP_VERSION` (Acerca de), `UVT`/`TOPES_RENTA_UVT` (usados por el monitor de renta que vive en `analisis`, no aquí). El monitor de renta (K.3) que consume `config.datosFiscales` y `config.perfilFiscal` vive en `analisis/logic.js` (`calcularEstadoRenta`), ver [`analisis.md`](analisis.md).

**TX.11 (2026-07-15), único switch de la app**: `.config-toggle` ya no estiliza el switch en sí (antes tenía su propio `input[type="checkbox"]` con `appearance:none` + `::after`); ahora solo es el contenedor "pill" (padding, fondo, hover) que envuelve `.toggle` de `atoms.css` (el mismo componente que usa D.14 en Deudas). Los toggles de tema (`#toggle-tema`) y notificaciones (`#toggle-notif`) en `_renderTema()`/`_renderNotificaciones()` (`config/view.js`) anidan `<span class="toggle"><input/><span class="toggle__track"></span></span>` dentro del `<label class="config-toggle">`. `config/index.js` (línea ~295) ajustó `toggle.parentElement` → `toggle.closest('.config-toggle')` porque el input quedó un nivel más profundo.

**Dependencias y relaciones**: `config/index.js` registra acciones (`exportar-datos`, `importar-datos`, `resetear-app`, `activar-notificaciones`, `toggle-notificaciones`, `exportar-gastos-csv`, `abrir-legal`) y maneja los `submit` de `#form-perfil`, `#form-perfil-fiscal`, `#form-datos-fiscales` directamente (muta `S`, `save()`, re-render). Los tres formularios del candado (`#form-bloqueo-crear`, `#form-bloqueo-cambiar`, `#form-bloqueo-quitar`, CFG.5a) son la excepción al chip de guardado: la tarjeta cambia de forma, así que re-renderizan el panel y confirman con `mostrarToast()`. Escucha `storage:error`/`storage:cuota` (ADR 030) y `theme:change`. `_exportarDatos` serializa `S` completo; `_importarDatos`/`_resetearApp` recargan la app. El perfil fiscal y los datos de renta que se guardan aquí los **lee** el monitor de renta de Análisis (ADN 10 respetado: Análisis lee `S`, no importa `config`). El Centro Legal (LEG.1) trae los `.md` de `docs/legal/` con `fetch` (mismo origen, precacheado por el SW) y los convierte con `infra/markdown.js`; no muta `S`, no usa EventBus.

**Riesgos**:

- **`perfil.smmlv` es dato muerto (CFG.1)**: ningún cálculo lo lee (la lógica usa la constante legal `SMMLV`). CFG.1a dejó de mostrarlo/editarlo pero lo **conserva en el estado** por compatibilidad de datos existentes y de los seeds E2E que lo setean. No re-introducir un campo de "salario" que nada consuma (fue justo lo que CFG.1 eliminó).
- **La situación laboral ya tiene consumidor** desde CFG.2b: `inferirEstadoDeclarante()` (en `analisis/logic.js`, no aquí) la usa para el encuadre del veredicto de renta. Sigue siendo campo controlado (solo ids de `SITUACIONES_LABORALES` o `''`); el id desconocido cae al encuadre genérico y el `''` invita a registrarla, así que un dato viejo o inventado nunca deja el veredicto sin segundo párrafo.
- **Los tres `submit` ya no re-renderizan el panel** (CFG.6): persisten, encienden el chip `#config-perfil-ok` / `#config-fiscal-ok` / `#config-renta-ok` unos 3 s y conservan foco y scroll. Los campos ya muestran lo guardado, así que no hay nada que repintar. **Sí siguen re-renderizando el panel entero** `_activarNotificaciones`, `_toggleNotificaciones`, `_toggleAvisoSeccion` (CFG.3c) y los listeners de `storage:error`/`storage:cuota`: ahí la tarjeta cambia de forma. Efecto conocido: un re-render cierra el `<details>` de Impuestos si estaba abierto.
- **`.config-info` ya solo aparece una vez** ("Acerca de"): el resumen `<dl>` del perfil se eliminó porque repetía los dos campos del formulario que tiene debajo. Si vuelve a haber dos, scopear al `section[aria-labelledby="..."]`.
- **De los tres campos de renta, solo dos siguen siendo la única fuente de su criterio (CFG.2a)**: "Ingresos brutos" es un override de la estimación que calcula Análisis, y el copy del bloque lo dice. Vaciarlo no apaga el criterio: lo devuelve a la estimación. Los otros dos (`consumosTC`, `consignaciones`) sí son la única fuente, y lo seguirán siendo mientras no exista tipo de cuenta "tarjeta de crédito" (MC.16).
- **El asistente fiscal (CFG.2c) conserva los tres campos de renta, no solo los dos que quedaron sin derivar.** El override de "Ingresos brutos" no se retiró: sacarlo habría dejado sin forma de corregir un año atípico a quien ya lo usa, y los `tip` de `calcularEstadoRenta()` siguen diciendo "registraste manualmente en Configuración". El modal (`#modal-fiscal`) es puro traslado de UX (D1 del ADR 050): ninguna pregunta cambió, solo dejaron de estar montadas en el panel en reposo.
- **Los tres campos de renta son `type="text"`, no `type="number"`**: guardan con separador de miles y se leen con `desdeMiles()`, que devuelve `null` para el campo vacío. Cualquier lectura nueva de `#df-*` debe pasar por ese par; leer `.value` crudo trae los puntos.
- **La corrección del checkbox (`flex-shrink` + `min-height: 44px`) está acotada a `#panel-config`**: la regla base sigue en `forms.css` con el defecto original, así que los `.checkbox-row` de Mis cuentas y Apartados con etiqueta larga se siguen deformando. Promoverla a `forms.css` es una tarjeta transversal, no de Ajustes.
- **Persistencia debounced (200 ms)**: un `reload()` inmediato tras guardar puede perder el cambio si el flush no corrió; en E2E, además, el `addInitScript` de `saltearOnboarding` resiembra `fk_v1` en cada carga (por eso la persistencia se verifica leyendo `localStorage`, no recargando).
- **El candado de acceso no cifra nada (CFG.5a, [ADR 063](../DECISIONS/063-candado-de-acceso-local.md))**: `fk_v1` sigue en texto plano y el DOM se pinta detrás del overlay. Protege de otra persona que use el dispositivo, no de devtools, de un respaldo exportado ni del perfil del navegador. La tarjeta de Ajustes lo dice con esas palabras: **no suavizar ese texto** sin cambiar antes la decisión del ADR. El PIN de 4 dígitos tiene 10.000 combinaciones: el hash evita que quede legible, no lo vuelve fuerte.
- **El freno de intentos vive en `sessionStorage`** (`fk_bloqueo_freno`), no en `S`: cerrar la pestaña lo limpia, y eso es deliberado (el límite real lo pone el punto 3 del ADR, no el freno). Cualquier lectura nueva del freno pasa por `msDeFreno()`/`registrarFallo()`, que aceptan el reloj inyectado para poder testearse.
- **`crypto.subtle` solo existe en contexto seguro**: abrir Finko por `http://192.168.x.x` deja el bloque de candado con un aviso en vez del formulario (`cryptoDisponible()`). No es un bug: es la única salida honesta.
- **El gate del candado va antes del gate legal en el arranque**: `bootstrap.js` retiene `initAceptacionLegal()` y las novedades detrás del evento `bloqueo:abierto`. Agregar un tercer overlay bloqueante obliga a entrar en esa misma cola, no a abrirlo en paralelo.
- **El interruptor por sección (CFG.3c) no tiene cobertura E2E de interacción**: Chromium headless devuelve `Notification.permission === 'denied'` aunque el contexto de Playwright conceda el permiso (`context.grantPermissions(['notifications'])` no lo mueve a `'granted'` en este entorno), así que la rama que pinta el `<fieldset>` nunca se alcanza en un test real de navegador. Mismo límite que ya tenía sin probar `toggle-notif`/`toggle-atajos`: cubierto por unidad (`config.test.js` stubea `globalThis.Notification` directamente).
- **Apagar una sección en Ajustes no actualiza en vivo el panel "Avisos" de Inicio**: `_toggleAvisoSeccion()` solo re-renderiza `#panel-config` (mismo patrón que `_toggleNotificaciones`/`_toggleAtajos`, ninguno emite `state:change`). El panel de Inicio lee la preferencia en su próximo render (navegar a `#dash`, recargar), no al instante. Consistente con el resto de `S.config`, no es una regresión de esta rebanada.
- **El Centro Legal depende de `fetch` en tiempo de ejecución**: si el `.md` no está precacheado y el dispositivo está offline en el primer uso, `_mostrarDocumentoLegal()` muestra un mensaje de error corto en vez de romper el modal (catch explícito). Los 10 documentos están en `CORE_ASSETS` del service worker: en producción, tras la primera visita quedan disponibles offline igual que el resto de la app.
- **Contenido en v0.1, no v1.0**: los textos de `docs/legal/` tienen marcadores `[PENDIENTE: ...]` (responsable, correo de contacto) visibles tal cual en el modal hasta que Esteban los resuelva; no es un bug, es el estado real del paquete (ver `docs/legal/README.md`).

**Cambios pendientes**: la iniciativa fusionada CFG.1+CFG.2 **cierra completa con CFG.2c**, **CFG.3 cierra completa con CFG.3c** y **CFG.6 cierra completa con su inventario** (las tres, 2026-08-13). Lo que queda de seguridad del panel: CFG.4 (respaldo/sync, roza ADN, sin iniciar), CFG.5b (re-autenticacion en acciones criticas: borrar todo, exportar, resetear, reusando `verificarPin()`) y CFG.5c (spike de biometria antes de prometerla). LEG.1 (Centro Legal) queda **cerrada**: falta resolver el checklist de contenido de `docs/legal/README.md` (LEG.2, bloqueada por eso, no por código).

**Cambios realizados**:

- 2026-08-13 (**CFG.6**, cierra la tarjeta): inventario final de configuraciones faltantes en Ajustes (punto 1, único frente abierto que quedaba). Revisado el panel completo contra las cinco tarjetas que lo tocan (CFG.1 a CFG.5): **sin hallazgos nuevos**. Lo único que falta en el centro de configuración ya tiene tarjeta propia (CFG.4 respaldo/sincronización, CFG.5b re-autenticación, CFG.5c biometría, LEG.2 contenido legal); el Bento Grid a escritorio sigue como mejora futura sin bloquear nada (medido en el pase 2026-08-02). Sin cambios de código: tarea de auditoría, área design, conclusión documental.
- 2026-08-13 (**CFG.3c**, cierra la iniciativa CFG.3, [ADR 066](../DECISIONS/066-motor-unico-de-avisos.md) nota de la misma fecha): "Recordatorios" gana un interruptor por sección (`_renderPreferenciasAvisos()`, cinco `.config-toggle` dentro de un `<fieldset>` sin CSS nueva más allá del layout, `reset.css` ya deja `fieldset`/`legend` sin caja), visible solo con los recordatorios activos. Cada `input` lleva `data-action="toggle-aviso-seccion"` + `data-seccion`, un solo handler (`_toggleAvisoSeccion()`) para las cinco filas. Motor: `filtrarPorPreferencia()` nuevo en `infra/avisos.js`, consumido tanto por `infra/notificaciones.js` (la notificación del sistema) como por `resumen/view.js` (el panel de Inicio de CFG.3b). Se suma el sello persistido `config.ultimoAvisoISO` (reemplaza el flag de sesión en memoria de `notificaciones.js`: antes cerrar y volver a abrir la app el mismo día repetía la notificación). Schema v39 → v40, migración aditiva (todas las secciones activas por defecto, sello en `null`). Copy de "Recordatorios" actualizado para no seguir hablando solo de compromisos. 15 tests unitarios nuevos (`avisos.test.js`, `storage.test.js`, `config.test.js`); 22 en `notificaciones.test.js` se simplifican (sale el reset del flag de sesión, ya no existe). SW v529 → v530.
- 2026-08-13 (**CFG.2c**, cierra CFG.1+CFG.2): "Perfil fiscal y datos de renta" deja de montarse en el panel de Ajustes, ni siquiera plegado (D1 del [ADR 050](../DECISIONS/050-perfil-fiscal-ubicacion-y-framing.md)). El grupo "Impuestos" pasa a una tarjeta con el botón "Completar perfil fiscal", que inyecta los mismos dos formularios (sin tocar su HTML ni su contrato de guardado) en un modal nuevo (`#modal-fiscal`) al abrirse. `renderModalFiscal()` (nueva, `view.js`) construye ese contenido; `_abrirPerfilFiscal()`/`_wireFormularioFiscal()` (`index.js`) lo inyectan y cablean los `submit` que antes vivían en `_inyectarPanel()`. Sin cambios de lógica ni de schema: los dos formularios siguen guardando en `S.config.perfilFiscal`/`S.config.datosFiscales` exactamente igual. 3 tests reescritos en `config.test.js`. SW v524 → v525.

- 2026-08-13 (CFG.2a): "Ingresos brutos" deja de ser captura obligatoria y pasa a override de la estimación que hace Análisis con `S.ingresos` + `S.ingresosPuntuales`. En Ajustes solo cambió texto: el encabezado del bloque ("Finko estima tus ingresos brutos...") y la ayuda del campo. La lógica vive en `analisis/logic.js` e `infra/financiero.js`, no acá.

- 2026-08-12 (CFG.5a): candado de acceso local con PIN de 4 a 8 dígitos (hash `SHA-256` + salt en `config.bloqueo`, schema v36), gate de arranque opaco y bloqueante, freno de 5 intentos en `sessionStorage`, salida "Olvidé mi PIN" que borra todo. Decisión y alternativas rechazadas (cifrado real, patrón, biometría): [ADR 063](../DECISIONS/063-candado-de-acceso-local.md).

- 2026-08-03 (CFG.2b): el checkbox "La DIAN me notificó como declarante" deja de ser captura y pasa a override positivo; el veredicto lo calcula `inferirEstadoDeclarante()` en Análisis. Resuelve D2 del ADR 050 (framing: alternativa C acotada).

- 2026-08-02 (CFG.6, pase de escritorio/tablet + tema claro): "Instalar Finko" y "Activar recordatorios" vivían sueltos dentro de `.config-section` (flex column sin `align-items`, stretch por defecto) y se estiraban a 932px en escritorio (1280px) y 660px en tablet (768px); el pase 2026-07-25 solo cubrió los botones de `.config-form`/`.config-danger` y los pares de `.config-actions--ambito`. Nueva regla `.config-section > .btn { align-self: flex-start; min-width: 160px; }` en `config.css`, mismo patrón que "Guardar perfil"; verificado en 390/768/1280px. Tema claro revisado por código (100% `var(--fk-*)`, cero color hardcodeado): sin captura visual real, el Browser pane no compuso frames en la sesión. El inventario de configs faltantes (punto 1 de CFG.6) queda fuera, depende de CFG.1 a CFG.5. 26/26 tests de `config.test.js` verdes.

- 2026-07-25 (CFG.6, auditoría de diseño de la sección: 11 de 13 hallazgos): el panel pasa de una pila plana de diez tarjetas iguales (`#panel-config` 3.810px, página 4.577px a 390px) a **cinco grupos rotulados** con `_grupo()` y el orden invertido por frecuencia de uso: el interruptor de tema baja de 1.580px a 114px y el panel a **2.357px (-38%)**. Perfil fiscal y datos de renta viven plegados en un `<details class="config-desplegable">` (cero JS), y con "Instalar Finko" y "Activar recordatorios" pasados a `.btn-secondary` queda **un solo `.btn-primary` visible en reposo** (regla R11). El resumen `<dl>` del perfil se elimina (repetía los dos campos de abajo). Los tres campos de renta pasan de `type="number"` pelado a `type="text" inputmode="numeric"` con separador de miles al escribir (`miles()`/`desdeMiles()` en `view.js`). El importador de respaldo deja de ser un `<label class="btn">` con `<input class="sr-only" aria-hidden="true">` que sí recibía foco (regla axe `aria-hidden-focus`) y pasa a un `<button>` real que dispara un `<input type="file" hidden>`; su acción `importar-datos` se registra en `initConfig()` y el `change` se cablea por `#config-importar-json`. Los cuatro botones de datos van en dos pares rotulados por ámbito ("Toda la app" / "Solo tus gastos") con verbos que dicen la consecuencia. La rama `denied` de notificaciones se ramifica con `estaInstalada()` (en PWA no hay barra de direcciones) y ofrece salida a Calendario. El Centro Legal destaca Términos y Privacidad y pliega los otros ocho (`destacado` en `legal.js`); el visor muestra `VERSION_LEGAL` como chip junto al título. "Acerca de" pierde la fila de tecnología y abre con la promesa de privacidad. El encabezado de `#sec-config` gana la teja `#i-ajustes` como las otras once secciones y salen los diez emoji de los `<h2>` y los seis de botones (los de `.nudge` y los de logros no se tocan: son otro patrón y otro dominio). 21 tests unitarios nuevos en `config.test.js`, 4 E2E ajustados en `smoke.test.js`. 3007/3007 unit + 231/231 E2E + lint verdes. SW v415 → v416. **Fuera de alcance**: B11 (sacar Logros de Ajustes, toca ADR 022/NAV2) y las decisiones V2 (autoguardado), V3 (buscador) y V4 (migración completa de emoji a sprite).

- 2026-07-15 (TX.11, consolidación del switch único de la app): `.config-toggle input[type="checkbox"]` (track propio con `appearance:none` + `::after`) se elimina de `config.css`; `_renderTema()` y `_renderNotificaciones()` (`config/view.js`) anidan el componente `.toggle` de `atoms.css` dentro del `<label class="config-toggle">`, que pasa a ser solo el contenedor "pill". `config/index.js`: el listener de `theme:change` que refresca el checkbox y su texto tras un cambio externo de tema ajustó `toggle.parentElement?.querySelector('.config-toggle__label')` a `toggle.closest('.config-toggle')?.querySelector(...)` (el input quedó un nivel más adentro). Mismo cambio migró el toggle D.14 de Deudas (`.toggle-switch` → `.toggle`, ver [`deudas.md`](deudas.md)) y le sumó a `atoms.css` el fallback `var(--fk-section-accent, var(--fk-accent))` en el estado marcado (Ajustes no vive dentro de un `[data-dom]`, así que cae al acento neutro, cero cambio visual aquí). 2 tests unitarios nuevos en `config.test.js` + 1 en `compromisos.test.js`; el E2E de tema (`smoke.test.js`) pasó de clickear `#toggle-tema` directo (ahora visualmente oculto dentro de `.toggle`) a clickear `label[for="toggle-tema"]` (reenvía el click al input anidado, mismo patrón que los chips). 2829/2829 unit + 207/207 E2E + lint verdes. SW v403 → v404.

- 2026-07-09 (LEG.1, rebanada de UI): apartado "⚖️ Centro Legal" en Ajustes (`_renderLegal()`), lista de los 10 documentos con `data-action="abrir-legal"`. Un solo modal genérico (`#modal-legal` en `index.html`) trae el `.md` correspondiente con `fetch` (mismo origen; el service worker ya los precachea, `docs/legal/*.md` sumados a `CORE_ASSETS`, SW v340 → v341) y lo convierte con un conversor Markdown propio nuevo (`infra/markdown.js`, sin dependencias, cubre solo el subconjunto que usan estos documentos: encabezados, negrita, código en línea, enlaces, listas, tablas, citas, `---`). Los enlaces `[texto](otro.md)` llevan `data-doc-link` y cambian de documento sin cerrar el modal (delegación de eventos en `_wireLegalLinks()`). 13 tests unitarios de `markdown.js` + 2 de la lista en `config.test.js` + 3 E2E reales (fetch contra los `.md` servidos, navegación entre documentos, cierre del modal). 2282/2282 unit + 158/158 E2E verdes.
- 2026-07-09 (LEG.1, rebanada de borradores; solo docs, cero código): redactado el paquete legal completo en `docs/legal/` (11 archivos: README con reglas de versionado y checklist de pendientes, términos y condiciones, política de privacidad, tratamiento de datos personales Ley 1581, aviso de cookies, descargo de responsabilidad, propiedad intelectual, aviso de marcas de terceros, licencias de terceros, aviso legal e historial de cambios). Modelo local-only vigente, con cláusula de cambio de modelo (CFG.4) en cada documento y formato ADN 11 ("En pocas palabras" + texto formal). Marcadores `[PENDIENTE: ...]` para responsable, correo de contacto y decisión de licencia del código; gate: revisión por abogado colombiano antes de pasar a v1.0.

- 2026-07-06 (CFG.1a, fusión CFG.1+CFG.2 slice 1): el encabezado del perfil mostraba nombre + "SMMLV configurado" con un campo editable, pero `S.perfil.smmlv` no lo leía ningún cálculo (dato muerto). Se quitó ese campo del encabezado (`_renderPerfil`) y se agregó **situación laboral** (`SITUACIONES_LABORALES`: empleado, independiente, pensionado, mixto, otro; `''` = sin especificar), persistida en `perfil.situacionLaboral` (schema v24 → v25, migración idempotente). El handler de `#form-perfil` valida contra el catálogo (nunca guarda un valor libre). Ficha nueva (primer análisis a fondo de la sección, regla 2.6). 9 tests nuevos (5 de render en `config.test.js` nuevo, 4 de migración en `storage.test.js`) + 2 E2E en `smoke.test.js`; `state.test.js` actualizado a la forma nueva de `perfil`. 2252/2252 unit + 155/155 E2E verdes. SW v335 → v336.
